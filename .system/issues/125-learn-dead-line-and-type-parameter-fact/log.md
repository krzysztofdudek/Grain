
## 2026-09-06 08:48 — Starting 125: merged claude/grain-agent-tool-b89y0x, branch fix/125-type-parameter-fact. Plan: (1) remove dead st2 line in learn.mjs, (2) add per-scope declared type-parameter fact derived from each grammar's own node-types.json (type_parameters/type_parameter nodes), thread through serializeScope/export additively, (3) measure ptype/returns/extends rows naming a declared type param across Yggdrasil/express/petclinic/grain in a scratch copy, (4) tests across >=4 grammars with generics.

## 2026-09-06 09:14 — Implemented the type-parameter fact. (1) Removed the dead 'const st2 = model.steers ? null : null' line and its stale comment in engine/learn.mjs — nothing read st2; the exemplar value a few lines below is read straight from prepared/preds exactly as the comment already claimed. Separate commit. (2) bindingFor (parse.mjs) now derives two grammar-native sets from each grammar's own node-types.json alone: tparamDecl (a node type IS one type parameter's own declaration — matches the whole word 'type_parameter', so it also catches Go's type_parameter_declaration and Scala's contravariant_/covariant_type_parameter, but excludes any name also matching constraint/constraints/modifier/modifiers/list/clause, so C#'s type_parameter_list/type_parameter_constraint/type_parameter_constraints_clause and Kotlin's type_parameter_modifiers never count as a declaration) and tparamContainer (the plural 'type_parameters' node every TS/Java/Kotlin/Rust/Scala field points at, plus the singular '_list' spelling C#/Go use). extract.mjs's new declaredTypeParams(ch, b) walks a scope's OWN header only (childForFieldName('type_parameters') first, else the first loose/unnamed child in tparamContainer/tparamDecl — needed for Kotlin/Scala functions and Go, which expose no such FIELD) and reads each declaring node's identifier off its own 'name' field (childrenForFieldName — handles Go's multiple-name-one-constraint '[T, U any]') or, where a grammar declares no field at all for it (Java/Groovy/Kotlin), the first named child whose type says identifier/name as a whole word. Scala's plain (unwrapped, no-variance) parameters are read straight off the CONTAINER's own name field before recursing, so they are never double-counted against the +/- variant nodes. Persisted as scope.tparams (extractScopes in scopes.mjs, both the noBody and normal scope shapes), threaded through serializeScope (partition.mjs; hydrateScope already spreads it through unchanged), and exposed additively in grain export's site() plus a new schemaNotes.tparams entry explaining how a consumer should read it (an argument equal to a site's own tparams, or its OWNER's for a member, names a type parameter, not a domain type). Verified byte-identical: grain export on a real fixture, diffed field-by-field ignoring only the new tparams key, the extractor version stamp and the run's own indexedAt timestamp — zero other differences. Confirmed tparams reaches tree.json (the EXTR_V-gated tree cache) but never model.json, so only EXTR_V needed bumping (g33->g34, changelog comment added in config.mjs), not MODEL_V — verified directly by grepping the rebuilt model.json (0 hits) and tree.json (23 hits on Yggdrasil) after rebuilding a fixture's store from the g33 cache and observing 'extracted ... 0 from cache, 1 parsed' (cache correctly invalidated) with model.json otherwise unaffected. (3) Measurement (raw scope-level ptype/returns/extends occurrences vs each scope's own tparams, or its owning type's for a member — a scratch copy per repo under scratchpad/w125/, no propose.mjs/mining involved, so history-independent and fast): Yggdrasil (970 files, 16181 scopes, 23 scopes carry tparams) — ptype 6/3789 name a declared type parameter, returns 2/2846, extends 0/46; spring-petclinic (77 files, 359 scopes, 2 with tparams) — ptype 0/96, returns 1/184, extends 0/17; express (148 files, plain JS, no static types at all) — 0/0 across all three, as expected; grain itself (1174 files, plain JS/mjs) — 0/0 ptype and returns, 0/1 extends. Every hit spot-checked against the real source (e.g. Yggdrasil's assertNotCancelled<T>(value: T | symbol), petclinic's EntityUtils.<T extends BaseEntity> T getById(...)) and confirmed correct. Numbers are small in absolute terms, consistent with 120's own finding that its guess-based class-2 guard never fired at certified/sub-gate thresholds on these same four corpora — generics are a small minority of ptype/returns/extends occurrences in real code, but the ones that exist are now named EXACTLY rather than guessed. FOLLOW-UP FOR PROPOSE.MJS (not made here, per instruction): in buildAspects(), replace looksLikeGenericTypeParam's form-guess (CONVENTIONAL_TYPE_PARAM_RE + declaredTypeNames) with an exact test — resolve the row's own host site's tparams array (now in the export) plus, when the host is a method/member (kind:'method' with .own set), its OWNER type's site.tparams, and treat identifierUnderTest's value as a type parameter iff it is in that union; drop CONVENTIONAL_TYPE_PARAM_RE and the declaredTypeNames census entirely once this lands, since the exact fact supersedes both — the guess was only ever a stand-in for exactly this data. (4) Tests: plugins/grain/tests/type-parameter-fact.test.mjs, 13 new tests, real snippets across all 7 requested grammars (TypeScript, Java, Kotlin, Rust, C#, Go, Scala) via the existing scopesOf() harness (declaration-extraction.test.mjs's own convention) — each asserts a type's/function's own declared set, a member's non-inheritance from its enclosing generic type, C#'s where-clause never contributing a phantom name, Go's shared-constraint multiple-name field, Scala's plain+variant mix, PLUS three absence tests (JavaScript and PHP have no type-parameter syntax at all -> always [], and a non-generic TS class/method pair) and a serializeScope/hydrateScope round-trip test asserting every pre-existing field name survives untouched. Noted, not fixed (out of scope for 125, pre-existing and unrelated to type parameters specifically): Go struct/interface type declarations (type Box struct{...}, generic or not) are never captured as a kind:'type' scope at all today — confirmed on a plain non-generic struct too, so it is a general Go type_declaration/type_spec extraction gap, not something this ticket's tparams change touches or could fix; Go's own tparams coverage here is limited to functions, which DO get captured.
