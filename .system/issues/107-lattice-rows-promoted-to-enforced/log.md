
## 2026-09-05 13:26 — Orzeczenie enforced-requires-certified-origin. Repro: YG_BIN=... node plugins/grain/bin/grain.mjs propose w /home/user/Grain → 'enforced: 22 of 81', candidates 0; wszystkie 22 mają provenance.origin=sub-gate-lattice, np. grain/claude/candidate-auto-imp-node-url share 0.8 n=8 deviating 2. Na Yggdrasil (latticeRows 0) wynik 10/124 nie może się zmienić.

## 2026-09-05 13:27 — Worker start: implementing enforced-requires-certified-origin ruling — origin sub-gate-lattice caps at advisory; candidates section = advisory aspects; 106 in passing.
## 2026-09-05 14:04 — Implemented ruling enforced-requires-certified-origin: promoteEnforceableAspects now gates enforced on origin certified-convention AND a clean drill; a sub-gate-lattice origin that clears the identical drill earns advisory instead (Yggdrasil: reviewer runs, refused -> warning, recorded in baseline). proposal.json/provenance.json status is now literally enforced/advisory/draft (was Grain's own active/draft translated at write time); counts gained aspectsAdvisory, kept aspectsActive/aspectsDraft names for schema stability. The quiet report's candidates section is now advisory aspects (strongest evidence first) with the older draft-caught-something definition folded in below them, each line naming its yg status word. Verified: Grain self-propose goes from enforced 22/candidates 0 to enforced 0/advisory 22/candidates 22 (all 22 origin sub-gate-lattice, none reaching enforced); a staged yg check on the result loads clean (PASS, no aspect-status-downgrade) and yg check --approve --only-deterministic records advisory refusals as warnings without blocking. Yggdrasil's own self-propose is unchanged in substance (124 aspects, 10 enforced, drafts prose 91/no-catch 22/fa 1, advisory 0 since latticeRows is 0 there) -- the only .yggdrasil/ tree diffs are provenance.json status active->enforced (10 files) and yg-aspect.yaml name field losing its mid-word truncation (23 files, ticket 106).
