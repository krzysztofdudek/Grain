
## 2026-09-05 21:31 — MEASURED (ticket 108 measurement half, worktree research/108-measure). The loss is three stages, not one; commands: node plugins/grain/bin/grain.mjs export --repo <petclinic 818c413> --out export-petclinic.json --compact --no-anchors (106 s), then a read-only probe over git ls-files + that export.

STAGE 0 — what the code contains. 50 Java files (30 under src/main/java, 20 under src/test/java). 24 internal import statements (10 from src/main, 14 from src/test). A further 31 file->file same-package reference pairs carry NO import at all, because Java needs none inside a package; the vendored extractor's uses() emits candidates for import_declaration and scoped_type_identifier only (engine/vendor/relations/extractors/java.mjs:65-120), so a bare simple-name reference to a sibling class is never a candidate. The hand oracle's own graph is built over 91 edges precisely because it counts those.

STAGE 1 — resolution: 10 of 24 internal imports resolve; grain export reports exactly 10 edges, all kind=import, all main->main. The 14 that drop are ALL test->main, and the cause is one function: resolveType in engine/vendor/relations/extractors/java-resolve.mjs:22-34 tries <ancestor>/<fqn-as-path>.java over the ancestor directories of the FROM file only. From src/test/java/org/springframework/samples/petclinic/service/ClinicServiceTests.java that is src/test/java/..., src/test/..., src/..., <root>/... — the sibling source root src/main/java is never tried. Maven and Gradle's standard two-source-root layout is therefore invisible to it. All 14 would resolve if the sibling source roots (src/main/java, src/test/java, both derivable from the tree) were tried after the ancestor walk. Not a one-liner and the file is generated/vendored ('do not edit'), so measured, not fixed.

STAGE 2 — module mapping: 0 of the 10 resolved edges survive. All 10 sit inside the single module src/main/java (30 files). moduleOf (engine/relations.mjs:375) is the first two path segments -> src/main; refineModOf (:386) refines a module holding >= max(40, 50% of indexed files) by ONE segment per round, at most 2 rounds. Indexed files = 77, so src/main (44) refines once to src/main/java (30 < 40, stop). Even without the threshold, round 2 would only reach src/main/java/org: Java's reverse-domain package prefix org/springframework/samples/petclinic is four further segments, so the depth-2 module rule plus two refinement rounds can never reach owner/, vet/, model/. This is structural, not a threshold to tune. The SAME 10 edges aggregated at Java-package granularity are 4 cross-package pairs (petclinic->model, petclinic->vet, owner->model, vet->model). moduleGraph.edges is therefore [] and moduleGraph.cycles [], which is where propose's '0 relations · 0 dependency cycles' comes from.

CROSS-CHECK against the hand oracle at ITS own granularity (reconstruct.mjs --graph oracles/spring-petclinic): 35 declared node pairs, grain 4 node pairs, matched 4 — recall 0.114, precision 1.000. So the edges grain does resolve are all correct and all present in the oracle; the fault is coverage, at every one of the three stages, never accuracy. relCoverage reports n=27 files in groovy/json/properties/yaml with no resolution, which is a different and smaller gap than any of the above.

REPORTING (the ticket's second half) stands unchanged and is now quantified: '0 relations' should read as 'this graph carries no law about dependencies' — on this repository the honest line names 24 internal imports seen, 10 resolved, 0 surviving the module cut.
## 2026-09-05 22:09 — start: worker on fix/113-java-relations; merged agent-tool base; reading 108 memo and relation layer

## 2026-09-05 22:20 — baseline re-measured on this branch: petclinic relation recall 4/35=.114, precision 1.000, 10 file edges, 10 modules, 0 module edges. Export baselines captured for express (32 mod/48 medges/153 edges), grain (20/6/1111), Yggdrasil (37/65/2161) to diff byte-identity after. Cherry-picked 108's two instrument commits (--graph, --score contentRoot) since they were not in the merge base.

## 2026-09-05 22:27 — stage 1 done (source roots). Derivation: package declaration as suffix of the directory (JLS 7.2.1, self-validating) UNION Maven/Gradle standard layout by directory shape (src/<set>/<lang> holding files of that language) — no build file read. Used for resolution (every source root, not only the file's ancestors) and for the module cut (below the source root, advanced through its non-branching reverse-domain prefix). petclinic: edges 10->24, modules 10->19, module edges 0->9, relation recall .114->.343, precision 1.000. express + grain moduleGraph/edges byte-identical; Yggdrasil edges byte-identical, module graph +3 nodes +1 edge, all inside its own java-ancestor-root-shadow fixture.

## 2026-09-05 22:36 — stage 2 done (same-package references). A simple type name becomes a symbol candidate keyed <package>.<Name>; the symbol table binds it only if that type is declared in that package, so JDK types, imported types and other packages stay silent. Shadowing honoured per spec: single-type import beats the package, a member type or type parameter of the same name beats it too. petclinic: file edges 24->97, module edges 9->14, relation recall .343->.800 (28/35). Precision 1.000->.718 — all 11 extras are real code the oracle does not declare (its UnitTests node declares no relations while OwnerTests names Owner; WebSliceTests declares the web layer but not the entities its MockMvc tests build). 880 relation tests pass.

## 2026-09-05 22:39 — stage 3 done (report wording). Single-line edit in engine/propose.mjs, proposeReport's architecture line (line 2520): when the relation count is zero it reads 'no law about dependencies could be mined (N references seen, M resolved, K survived the module cut)'; byte-identical otherwise. Numbers come from a new additive export field relStages {seen,resolved,crossing}, computed in core.mjs beside buildEdges and documented in export.mjs's schemaNotes — 'references' rather than 'imports' because a same-package Java reference is a dependency with no import to count.

## 2026-09-05 22:59 — stage 4 (measure). petclinic vs hand oracle at node granularity: recall 4/35=.114 -> 29/35=.829; precision 1.000 -> .725. File edges 10 -> 98, modules 10 -> 19, module edges 0 -> 14. All 25 class-(a) misses from the 108 ledger are recovered (8 test->main imports by source roots, 16 same-package type references, 1 static receiver); the 6 that remain are exactly the ledger's 3 (b) + 3 (c): db-scripts->owner/entities and db-scripts->vet/entities and web-slice->unit are edges the oracle README itself calls 'edges a miner reading only code will correctly not find', and the three ui/templates edges are a view name returned as a String literal. The 11 extras are all real code with no import because the package spans main and test (OwnerControllerTests:71 'private OwnerRepository owners', :74 'Owner george = new Owner()', ValidatorTests:52 'Person person = new Person()', CrashControllerTests:33, VetTests:30) against oracle nodes that declare no relations at all (UnitTests) or only the web layer (WebSliceTests). The Vet/Specialty trap now passes by sight: Vet->Specialty emitted (4 code references), Specialty->Vet not (javadoc only). propose on a petclinic copy: 22 node types, 28 nodes, 14 relations, 0 cycles (was 12/14/0/0); yg check PASS, 28 nodes, 131/133 files, 18 aspects, exit 0.

## 2026-09-05 23:02 — VERSIONS — measured, not read: with a .grain cache written by the pre-change engine, the new engine reproduces the OLD numbers exactly on spring-petclinic (10 file edges, 10 modules, 0 module edges instead of 98/19/14). Relation facts ride the tree cache (core.mjs extractTree serves relOut[rel] = hit.r from the cached entry) and that cache is gated by EXTR_V; the model is gated by MODEL_V. So an adopter upgrading in place sees no improvement at all until something else forces a re-index. Recommendation for the director: bump EXTR_V g32 -> g33 (per-file relation facts changed for Java) AND MODEL_V m25 -> m26 (model gains srcRoots and relStages, and every module id now depends on srcRoots — a store without it computes the old cut against a newly stored moduleGraph, exactly the G11 inconsistency m15 fixed). Not bumped here, per the ticket. Module graph content changes for Java repos by design; on express and grain moduleGraph is byte-identical, on Yggdrasil it gains 3 nodes and 1 edge inside its own java-ancestor-root-shadow fixture.

## 2026-09-05 23:10 — end: npm test in plugins/grain with YG_BIN and HORDE_DIR — # tests 2346 / # pass 2346 / # fail 0 (baseline 2335; +5 new tests here, +6 from the two cherry-picked 108 instrument commits). Five commits on fix/113-java-relations: source roots; same-package type references; report wording; static-call receivers; docs. Derived source roots per repo: express [] and grain [] (module graph byte-identical), petclinic [src/main/java, src/test/java], Yggdrasil [source/cli/tests/fixtures/java-ancestor-root-shadow/src, .../src/main/java] — both inside its own Java fixture.

## 2026-09-05 23:41 — 116's test adapted after the refresh — case (1), not a regression. Rendered the fixture: the five Java packages under src/main/java/app/ each become a module under the source-root cut, so grain publishes owner as an ACTIVE node type src-main-java-app-owner with when.path 'src/main/java/app/owner/**' and evidence 'selects 5 of 26 tracked files; exactly the 5 the evidence names (Jaccard 1.00)'; alternatives.md is empty, which is why the old ownerAlt() found nothing. 116's intent is delivered one step earlier and was verified end to end: staged the proposal unchanged, added an unseen src/main/java/app/owner/OwnerScheduler.java, yg check PASS 9 nodes, yg context --file answers src-main-java-app-owner. Only tests/propose-domain-cut-predicate.test.mjs changed: it now reads the domain from the architecture first and alternatives.md second, asserts the path form and the evidence for whichever shape it found, fails loudly naming both places if it is neither, and the third test skips the activation step when the type is already active. No engine change. npm test in the foreground with YG_BIN, HORDE_DIR and GRAIN_CORPUS_CLONES: # tests 2372 / # pass 2372 / # fail 0.
