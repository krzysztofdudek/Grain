
## 2026-09-06 09:18 — Seam maps done, appended to .system/research/core-split-plan.md as a new section. propose.mjs 265284 chars / 125 top-level blocks -> 16 modules + a facade (largest propose-aspects 36445, propose-write 36266; smallest propose-sizing 3226; facade 6933), zero import cycles. grain.mjs 160272 / 67 blocks -> 9 modules, grain.mjs keeping main and staying the dispatcher at 40559 (largest sibling grain-check 34230), zero cycles.

ONE METHOD CORRECTION FROM 117, and it matters. 117 found top-level blocks with a line regex. That is unsafe on these two files: both embed whole JavaScript files inside template literals (the check.mjs sources renderCheck generates, the drill corpora, the node-type fixtures), and inside those literals 'export function check(...)' stands at column 0. The regex read four such lines in propose.mjs as top-level declarations named 'check' and would have cut a module in the middle of a string. The splitter was rewritten on the engine's OWN vendored tree-sitter — a block is a top-level named child of the root plus the comment lines directly above it. Exact counts: 125 blocks in propose.mjs (the regex claimed 134), 67 in grain.mjs. Pure-move verifier already run against a preview generation of both splits: 125/67 blocks, 0 missing, 0 duplicated, 0 lines that are not a moved block or an import/export/comment.

Two cuts were moved to kill a cycle rather than following the section headings blindly. In propose.mjs, contentMd/subGateMd/mdTable first sat with the other markdown renderers, which made propose-aspects -> propose-markdown -> propose-aspects (aspect bodies are markdown; renderProposalMd words a row with describeRow); they became propose-drills, below the report markdown. In grain.mjs, readSeeds/hashSeeds/loadScopes sat in the seeds/export sections but are read by ensureFresh and by three commands each, making grain-context -> grain-seed -> grain-context; they belong with the store and go to grain-context. Also checked before cutting, because it is the one construct whose value depends on which file it stands in: import.meta.url is read in both files (here/CORE/BIN, PLUGIN_ROOT/BIN) and every new module sits in the SAME directory, so dirname(fileURLToPath(import.meta.url)) resolves to the same string.

Staging (one commit each): 1-4 propose (base/levels/types/nodes; lattice/checks/classify/sizing; aspects/status/family/charters; drills/markdown/report/write + facade), 5-6 grain (context/where/what/check; seed/export/report/session/usage + dispatcher). Gate per stage: the full 445-file proposal tree + proposal.json + quiet report from the stress wrapper on Yggdrasil, grain propose --json on a FROZEN clone of this repository (frozen deliberately: a proposal run on the live worktree mines the very code the stage is changing), grain export --json on three corpora after wiping .grain/ (a reused index would be a false green — ensureFresh is one of the things that moves), and where/check/check --all/report/rules/map/explain/obligation/completeness/status/selftest/how/what/review/spectrum in text AND --json on fixture, express clone and repo clone. Which fields vary between two runs of the UNCHANGED tree is being measured first, so the gate can tell drift from noise.
