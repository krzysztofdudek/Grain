
## 2026-09-02 13:00 — 0 answer-changed-diff in 25 with-arm runs. Placement identical to the without-arm in 5/6 pairs — nothing for an answer to fix. Redesign the instrument: (1) from the corpus repos' history, pick commits where the without-arm's replay diverges from the historical commit (wrong directory, missing co-changed file, missing test) — those are the tasks where a correct answer CAN change the diff; (2) verify the without-arm fails on them first (2 reps), then run the with-arm; (3) same metrics, same extractor, same privacy constraint. A trial that cannot show an effect even in principle is not an instrument. Opus design; reuse .temp/stress/h040/.

## 2026-09-02 13:28 — DIRECTOR REWRITE (2026-09-02, after the user's challenge). The last 25 runs measured tasks agents already get right in ~16 pre-write calls; the G catalog's market is the ~99-call runs where agents flounder. Two hard steps, in order, BEFORE any capability work: (1) HARD TASKS ONLY — take the realistic, long runs from the catalog's corpus (.temp/stress/trials, the without.jsonl runs with the highest pre-write counts; and/or new replays of commits whose without-arm diverges from history), run the without-arm 2 reps each; keep only tasks where the agent demonstrably fails or floundering is measurable (wrong directory, missing partner file, missing test, >60 pre-write calls). (2) PER-RUN COUNTERFACTUAL — for every kept without-run: where did the agent go wrong or slow, at which tool call, how many calls did it burn, and what ONE sentence at that moment would have turned it — from ANY source, not only grain. Only then: can grain produce that sentence today (which command, which data), cannot but the model holds the data, or cannot at all. Deliverable: the counterfactual table + the with-arm run on the SAME kept tasks + answer-changed-diff. If the counterfactual list is empty (agents recover on their own), say so — that falsifies the oracle-form hypothesis and the director takes a different product form to the user (hook-driven guard, not an oracle). Same privacy constraint.
