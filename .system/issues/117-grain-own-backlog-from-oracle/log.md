
## 2026-09-05 21:24 — Worker starting small mechanical items: raw control bytes (core.mjs:2629 + 2 history tests), guardian test for run-corpus.mjs, vendor network-calls verdicts. core.mjs 569k size item explicitly out of scope for this worker.

## 2026-09-05 21:34 — vendor/network-calls-are-browser-only reviewed: all 5 sites in plugins/grain/engine/vendor/web-tree-sitter/web-tree-sitter.js (fetch:1490,1607,1750; new XMLHttpRequest:1580,1593) sit behind an ENVIRONMENT_IS_NODE / ENVIRONMENT_IS_WEB-WORKER platform branch or an explicit !ENVIRONMENT_IS_NODE guard; ENVIRONMENT_IS_NODE is always true when grain runs as a Node CLI, so every site is dead code on this platform (verdict a, browser-only, matches the aspect's own recorded finding). No code change — vendored file, per project policy not hand-edited.

## 2026-09-05 21:51 — Small items done. (1) Escaped raw control bytes: core.mjs:2629 (SOH in comment), history-large-state.test.mjs:56/77/87 (3x SOH in string/comment), history-path-quoting.test.mjs:71 (SOH in string) — all to \x01, semantics identical, tests pass (7/7). Extended no-nul-bytes.test.mjs to reject any C0 control byte except tab/LF/CR across all .mjs under plugins/grain, excluding only the oracle's own violates-raw-soh drill fixture (deliberately keeps one SOH to test the aspect's check.mjs); test-first verified (fails pre-fix naming exactly the 3 offenders, passes post-fix). (2) Added tests/run-corpus.test.mjs (21 cases) guarding tests/stress/run-corpus.mjs, the one stress instrument with no guardian test: exercises real corpus.json validation, --only selection, the 11 ladder command shapes, failure classification, result-row merging, --table formatters, and the CLI's usage/error paths — no clones, no grain spawns, no network. Required a byte-identical-behavior refactor (proved via before/after stdout+stderr+exit-code diffs on 4 invocation shapes): added the same direct-invocation dispatch guard reconstruct.mjs/too-much.mjs already use, exported the pure helpers, extracted selectEntries/upsertRepoInto, hoisted LADDER_COMMAND_LABELS. (3) vendor/network-calls-are-browser-only: all 5 sites confirmed browser/worker-only dead code under Node (see prior log entry) — no code change, verdict recorded. (4) Staged the fixed tree with the oracle's .yggdrasil and ran yg check --approve --only-deterministic: source/no-raw-control-bytes 3 refused -> 0; stress/every-instrument-has-a-guardian-test 1 refused -> 0; vendor/network-calls-are-browser-only stayed 5 refused (expected, unfixed); engine/file-size-budget stayed 3 refused (out of scope, core.mjs untouched). Noticed but NOT touched: docs/internal-links-resolve now shows new errors on docs/results.md linking to .system/issues|research files added since the oracle's snapshot commit — pre-existing drift from the oracle being a frozen artifact, unrelated to this ticket. npm test (YG_BIN+HORDE_DIR set): 2311 tests, 2311 pass, 0 fail. core.mjs 569k-char size item (11.4x budget) intentionally left for the owning worker.

## 2026-09-06 04:57 — Seam map done, written to .system/research/core-split-plan.md. core.mjs = 573804 chars, 23 of its own section headings, 242 top-level declarations. Measured every declaration's char weight and the reference graph between them, then chose 27 modules so that no module exceeds the oracle's 50 000-char budget and the file-level import graph between them is a DAG (cycles: 0, verified mechanically). Nothing renamed: core.mjs becomes a re-export facade carrying exactly the names it exports today, so grain.mjs / export.mjs / propose.mjs / history.mjs / the instruments / the tests change no import in this ticket. One finding that changes the shape of the job: learn is a SINGLE 71 598-character function, so no pure move can bring it under budget - there is nothing inside it to move except statements. It is the only place where statements get lifted into functions of their own (five blocks, largest first: the relation layer ~9k to arch.mjs as applyRelationLayer, implications-per-group ~6.9k, change archetypes ~4.9k, steers ~4.4k, message affinity ~3.5k), each taking its free variables as explicit parameters, no early exit, nothing returned; that leaves learn at ~43k. Verified before touching anything: no cache key embeds a module path or a function's source - EXTR_V/HIST_V/MODEL_V are literal string constants in config.mjs read by history.mjs and grain.mjs, so a pure move must not and will not move them. Staging: (1) base/parse/extract/scopes/superposition/lexical/facts, (2) obligations/mine/weights/verbalize/partition, (3) placement/arch/check/spectrum/cards, (4) where/how/evidence/what/evals/report-facts/report/completeness/harness + core.mjs becomes the facade, (5) learn.mjs with the five lifts. Every stage gated on byte-identical grain export JSON (repo tree, fixtures, a real third-party clone) plus identical where/check/report text on a fixture, plus the full suite.

## 2026-09-06 07:09 — Stages 1-5 landed (f410583, 5ce16a2, c28b65f, 695d2f2, d74e547), all pure moves. core.mjs 573804 -> 6707 chars: it is now a re-export facade with no statement of its own, and every name it exported before is still exported from it, so grain.mjs / export.mjs / propose.mjs / history.mjs / the instruments / the tests import exactly what they did. 26 sibling modules carry the engine; the file-level import graph between them is a DAG with zero cycles. Every stage gated the same way and passed: grain export JSON identical before and after on three corpora (a real git clone of this repository, the built test fixture, an express clone), plus where/check/check --all/report/rules/map/explain/obligation/completeness/status/selftest text on the fixture and where/report/map on each clone - 23 of 23 artefacts byte for byte, the only difference anywhere being the index's own wall-clock build stamp, which differs between two runs of the UNCHANGED tree too (measured first, so the gate could tell drift from noise). Each stage also ran the full suite: 2387 pass, 0 fail, four times over. Method note worth keeping: the first attempt at stage 1 computed each module's imports with regexes over the source text and shipped a module missing one import (resolveImport), which turned extraction into a silent zero-scope run rather than an error - a regex string-stripper had swallowed a line after a regex literal. Redone with the engine's OWN vendored tree-sitter parsing core.mjs and reading identifier nodes, which is exact; a mechanical verifier then proved the pure-move property (236 top-level blocks, 0 missing, 0 duplicated, 0 lines in any generated file that are not a moved block or an import/export/comment). One thing left: learn is a single 71.6k-char function, so learn.mjs is 73501 chars and still over the 50000 budget. Stage 6 lifts seven statement blocks out of it into functions elsewhere - the relation layer to arch.mjs, group implications and structural twins to partition.mjs, the language bridge / concepts / change archetypes to a new commit-log.mjs, steers and waivers/boundaries to a new decisions.mjs - each taking its free variables (measured, 2-8 each) as explicit parameters.

## 2026-09-06 07:57 — Stage 6 + the CI gate landed (a134d80, 87de53a). core.mjs 573804 -> 6708 chars, 29 engine modules, largest scopes.mjs 45505, learn.mjs 40221, ALL under the 50000 budget.

Oracle gate (staged copy of the worktree with the oracle's own .yggdrasil at its root, yg check --approve --only-deterministic). Refused deterministic pairs, before -> after: engine/file-size-budget 3 -> 2 (core.mjs gone; grain.mjs 159454 and propose.mjs 247131 remain, both outside this ticket - propose.mjs belongs to 120); docs/internal-links-resolve 1 -> 1 (pre-existing drift); release/single-version-of-record 1 -> 1 (pre-existing: .codex-plugin and .cursor-plugin still say 0.3.0 while package.json / .claude-plugin / config.mjs say 0.4.0); vendor/network-calls-are-browser-only 1 -> 1 (pre-existing); relation-undeclared-dependency 1 -> 1 (pre-existing). NO new aspect refusal.

One new aspect refusal DID appear on the first attempt and was fixed the way the brief says. engine/no-test-or-instrument-import refused core.mjs because it imported './harness.mjs', and that rule refuses any shipped module importing a *harness.js file. The rule is right; the file name was wrong. engine/harness.mjs is now engine/selftest.mjs - it holds the mutation harness and the extraction-recall oracle behind `grain selftest`, so the verb it serves is the better name anyway.

TWO THINGS FOR THE DIRECTOR.

(1) yg check now reports 29 type-strict-orphan errors. Every new engine module satisfies the oracle's `engine-module` type - its `when` predicates classify all of them correctly, no file landed in the wrong type - but no node in the oracle owns them, and `engine-module` is enforce: strict, so a matching file needs a node mapping. The oracle is NOT wrong: it is a frozen artifact written against 3d249bf and simply predates these files. I did not edit it. The fix, if you want it, is to add the 29 module paths to `mapping:` in .yggdrasil/model/plugin/engine/core/yg-node.yaml (whose description also still says "a single 572 KB module"), with a provenance note. Be aware that this changes the node/mapping shape the reconstruction instrument scores grain's proposal against, which is why it is your call and not mine.

(2) Bug found while reading, logged and deliberately NOT fixed (a move refactor with fixes inside is unreviewable): in the cross-cell contested-marking loop - now engine/learn.mjs, immediately after the applySteers call - stands `const st2 = model.steers ? null : null;`. It is always null, never read, and its comment says "steers not built yet" although model.steers is built on the line above. A dead line plus a stale comment.

Byte-identity held through every stage including the lift: 23 of 23 artefacts identical to the pre-split baseline on all three corpora. grain's own moduleGraph: 20 nodes, 6 edges, 0 cycles, identical before and after; the file-level import graph across the 29 engine modules is also acyclic, checked mechanically after every stage. Final suite: 2390 tests, 2390 pass, 0 fail (2387 plus the 3 new budget cases).

## 2026-09-06 08:25 — Director ruling applied: the oracle graph now follows the engine (d5ff140, one commit, on the refreshed base 4557d36).

WHAT CHANGED. The `mining-core` type's `when` names the whole engine instead of one path, written as a complement (everything under engine/ except the seven modules that are something else: dispatcher, constant table, history walker, proposal writer, relation pass, export serializer, Yggdrasil-graph reader), so a module split out tomorrow is classified the day it lands. `engine-module`'s `when` names its three modules directly - now the shorter form. `mining-core` gained itself in its `calls` allow-list, because the facade calls the layers and the layers call each other. The Mining Core node became four ownership-sized nodes: Engine Facade (core.mjs, 6 683), Mining (14 modules, 263 726), Query Implementations (12 modules, 265 025), Model Assembly (3 modules, 58 209). README carries a dated "Updated since" note pinning every number in it, and every number in the 108 measurement, to the graph as of 3d249bf.

WHY NOT ONE NODE, as the brief assumed. I tried it first and measured it: one node owning all thirty files produces 29 type-when-mismatch + 29 type-strict-misplaced (a node of type mining-core cannot own files whose `when` selects engine-module - which is why the type's `when` had to move too), and once the types agreed, one node of ~582 000 characters broke the reviewer prompt ceiling on the three prose rules mining-core carries (prompt-too-large x3, a new blocking group). The facade also has to be its own node: it re-exports from every module, so any node containing it and them is a cycle. Four is the smallest cut with none of those. Node graph is acyclic; largest node ~265 000, well under the 600 000 ceiling.

GATE (3). Staged copy of the worktree with the oracle at its root, `node /home/user/Yggdrasil/source/cli/dist/bin.js check`:
  yg check: FAIL  45 nodes · 1542/1542 files (1542 node-owned, 0 type-covered, 0 excluded) · 30 aspects · 0 flows · 1 draft
  Errors (1683) in 2 groups: unverified 1682 pairs / 26 nodes; relation-undeclared-dependency 1 pair / 1 node
  Warnings (317) in 2 groups: unverified 316 pairs / 23 nodes
The pre-split baseline for the same plain check was: 42 nodes, 1510/1510, Errors (1526) in the SAME two groups - unverified 1525, relation-undeclared-dependency 1. So the group set is unchanged: type-strict-orphan 29 -> 0, and type-when-mismatch / type-strict-misplaced / prompt-too-large never appear. The one relation error is the pre-existing tests/suite -> propose + yggdrasil-graph pair, identical text before and after. The unverified count grows only because there are 32 more files and three more nodes carrying the same aspects.
With --approve --only-deterministic the picture is the pre-split set minus the one this ticket fixed: 1970 deterministic verdicts, refusals = docs/internal-links-resolve (results.md), engine/file-size-budget on grain.mjs and propose.mjs, release/single-version-of-record, vendor/network-calls-are-browser-only. file-size-budget 3 -> 2.

TESTS (4), foreground: 2398 tests, 2397 pass, 1 fail. The failure is NOT from this work and is not from ticket 120 either - it is `propose-domain-cut-predicate.test.mjs` / "the predicate classifies a file grain never saw". Measured: it reproduces in a copy of this worktree with the PRISTINE pre-117 oracle graph restored AND the pre-merge propose.mjs in place, so neither change causes it. The cause is environmental: /home/user/Yggdrasil/source/cli/dist/bin.js was rebuilt at 08:11 today, after my last green run (2390/2390 at 78d9678), and its `yg check` output now carries a coverage hint naming `.yggdrasil/yg-config.yaml` plus a notice that the run could not be measured against `main`. The test asserts `!/architecture-invalid|type-when-mismatch|node-invalid|yaml|schema/` over the whole output, so the bare substring "yaml" in an unrelated hint reads as "the domain type broke the graph". The check itself PASSED in that run. The fix is to tighten that assertion to the diagnostic identifiers it means rather than the word `yaml` - a separate ticket, not this one. The three new budget cases pass (ok 571/572/573).

ONE THING LEFT FOR THE DIRECTOR: `reviewer.tiers.standard.max_prompt_chars` is still 600000. It was set by a 572 KB single file that no longer exists; the largest node now costs ~265 000. I updated the comment to say that and left the number alone, because moving a check parameter is a maintainer decision, not a refactor's side effect. Lowering it to ~300 000 would be true today and would cost nothing.
