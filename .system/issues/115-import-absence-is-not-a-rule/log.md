
## 2026-09-05 22:11 — START: branch fix/115-116-119-selection. Target: a false-direction row (expected:false) from origin sub-gate-lattice in a class where 'expected:false' means 'does not use X' (imp/call/deco/extends/returns, plus prose-only has/ptype) is an ABSENCE, not a prohibition — it must never reach advisory. Plan: buildAspects marks it, renders no check.mjs (prose instead), pre-sets draftReason absence-not-forbiddance, and words the statement as an observation; a certified-convention false-direction row stays eligible but carries direction: absence in provenance.json.

## 2026-09-05 22:59 — DONE. Rule implemented as the director set it: for a class where expected:false spells 'does not use X' (imp/call/deco/extends/returns, plus the prose-only has/ptype), a false-direction row whose ORIGIN is sub-gate-lattice renders no check.mjs, ships as prose, is worded as an observation ('20 of 28 files under `src/**` do not import `node:fs` — an absence, not a rule'), and is held at draft with draftReason absence-not-forbiddance, pre-set in buildAspects; the one shared line in promoteEnforceableAspects now keeps a reason an aspect already carries (a || b). A certified false-direction convention stays eligible; provenance.json gains direction: 'absence' either way. proposal.json counts them separately (aspectsAbsenceNotForbiddance) and REFACTOR-BACKLOG.md lists such a row as an observation with '—' instead of 'never import X / 8 sites to fix'. MEASURED (propose, subgate cap 1000, YG_BIN set), advisory total and the false-direction lattice rows inside it, before -> after: spring-petclinic 33 advisory of which 30 absence -> 2 advisory, 0 absence; express 7 of which 4 -> 3, 0; Yggdrasil 71 of which 52 -> 21, 0; gin 22 of which 10 -> 3, 0; groovy-spock 12 of which 6 -> 5, 0. ENFORCED rows: unchanged everywhere by this ticket (petclinic 0->0, express 1->1, gin 1->1, kotlin 1->1, leveldb 0->0, Yggdrasil 8->8 and the same 8 ids). Standing refusals on untouched petclinic HEAD (stage, promote every deterministic aspect to advisory, yg check --approve --only-deterministic): 176 over 39 units -> 16 over 16 units, and 38 deterministic aspects -> 7. New test tests/propose-absence-not-forbiddance.test.mjs (5 cases) on a real fixture that mines both directions of one import class from one partition; tests/propose.test.mjs's ticket-109 wording case updated because that exact sentence ('No method under src/** may import lodash.') is the one this ticket forbids.
