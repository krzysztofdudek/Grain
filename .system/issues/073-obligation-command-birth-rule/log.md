
## 2026-09-02 03:48 — SPEC = .system/research/obligations-design.md §6 (ticket-ready), §3 (measurement), §2 (why two labelled sets), §4 (instrument). Summary: history.mjs stops discarding the A/M/D/R status byte (carry into persisted footprint; renames resolved so R is not a birth) → HIST_V bump; core.mjs learn() derives a table keyed on (refined module, suffix) of the ADDED file, gated by existing KT+BIC base-rate contrast, λ display bound, CFG.minRaw, liveness → MODEL_V bump; new command obligation <path> [--top N] [--json] with two labelled sets (specific / ambient), honest negative naming the class and its birth count; check --as <path> gains the line; map --json gets the table. selftest --obligation instrument with the 069 own-commit guard. Eight tests as listed in §6. Measured: 94.2% pooled / 81.1% repo-macro on 9.6% of new-file events vs 0.143 hot-file null; acceptance is written against the MACRO figure (pooled is curl-dominated: 139 of 223). Coverage 6/20 repos fire — disclose, do not inflate. VERSION BUMPS ARE THE DIRECTOR'S: worker must not touch config.mjs; report that HIST_V and MODEL_V are needed. Cost figures (index time, store rows) must come from a real run before merge.

## 2026-09-02 05:31 — CORPUS MEASUREMENT (7 repos, selftest --obligation --last 300, private scratchpad copies): c-curl coverage 0.237 p@1 1.000 non-obvious 69/69; cpp-json 0.053 / 0.875; flask 0.003 / 1.000; express, sinatra, gin, petclinic: 0 rules fire. POOLED n=1822, 88 fired: coverage 0.048, p@1 0.977, non-obvious 70/70 = 1.000, hot-null 0.193, random-null 0.034. REPO-MACRO (3 of 7 fired): p@1 0.958, non-obvious 1.000. Cost: table 2–15 ms / 5–21 KB per repo (the minutes are the history walk grain already pays). okhttp swapped for petclinic (partial clone correctly refused). VERDICT: precision bars (0.80) cleared with room; COVERAGE 0.048 does NOT clear the design's 0.08. Two testable hypotheses from the measurer: (1) the prospective harness demands ≥6 births before a rule may fire — stricter than the doc's 80/20 split; (2) the mandated (refined module, suffix) key collapses directories deeper than two segments, against the doc's own finding that finer keys win. DIRECTOR RULING: merge — a rule that speaks on 4.8% of births at 0.96–1.00 precision is strictly better than silence and makes no false claim; coverage is DISCLOSED in the command doc and validation.md as measured, and the two hypotheses are ticket 077. Do not widen the key or loosen the floor inside 073.
