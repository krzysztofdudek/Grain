
## 2026-09-02 03:48 — SPEC = .system/research/obligations-design.md §6 (ticket-ready), §3 (measurement), §2 (why two labelled sets), §4 (instrument). Summary: history.mjs stops discarding the A/M/D/R status byte (carry into persisted footprint; renames resolved so R is not a birth) → HIST_V bump; core.mjs learn() derives a table keyed on (refined module, suffix) of the ADDED file, gated by existing KT+BIC base-rate contrast, λ display bound, CFG.minRaw, liveness → MODEL_V bump; new command obligation <path> [--top N] [--json] with two labelled sets (specific / ambient), honest negative naming the class and its birth count; check --as <path> gains the line; map --json gets the table. selftest --obligation instrument with the 069 own-commit guard. Eight tests as listed in §6. Measured: 94.2% pooled / 81.1% repo-macro on 9.6% of new-file events vs 0.143 hot-file null; acceptance is written against the MACRO figure (pooled is curl-dominated: 139 of 223). Coverage 6/20 repos fire — disclose, do not inflate. VERSION BUMPS ARE THE DIRECTOR'S: worker must not touch config.mjs; report that HIST_V and MODEL_V are needed. Cost figures (index time, store rows) must come from a real run before merge.

## 2026-09-02 04:56 — Implementation complete: grain obligation <path>, check --as obligation line, map --json obligations table, selftest --obligation instrument. HIST_V/MODEL_V bump needed (added field / model.obligations table), not applied here per director's batching. Full suite 2094/2094 green, commit 4edd709. Real-corpus coverage/precision/cost measurement running via a fork; will append results.

## 2026-09-02 05:31 — Real-corpus measurement (7 repos: c-curl, cpp-json, flask, express, sinatra, gin, spring-petclinic; okhttp swapped out — partial clone, grain's own pre-existing guard correctly refuses it): pooled coverage 88/1822=0.0483 — DOES NOT CLEAR the 0.08 floor (design doc's own 20-repo prelim measured 0.096-0.122; gap plausibly from (a) this instrument's stricter per-event leave-one-out — a class needs >=5 STRICTLY PRIOR same-class births, so its 5th birth can never fire, vs the design doc's single 80/20 split where all scored events share the full 80% training set, and (b) only 7 of 20 repos sampled, missing several of the design doc's highest-coverage repos). Precision clears comfortably: repo-macro precision@1 0.9583 (3 of 7 repos fired: c-curl 1.000/71, cpp-json 0.875/16, flask 1.000/1) vs >=0.80 bar; pooled precision@1 0.9773; non-obvious precision 1.000 (70 events) vs >=0.80 bar. Hot-file null on the fired subset: pooled 0.193 (c-curl alone, the trustworthy large sample, is 0.113 — under the doc's 0.15; the two tiny-sample repos pull the pooled figure up). Cost: buildObligationTable itself 2-15ms per repo (negligible vs the multi-second-to-15-minute history walk/blob parse learn() already pays); model.obligations on disk 5-21KB per repo. Per the ticket's own instruction: reporting this honestly rather than forcing a ship-as-fully-validated claim — coverage is a genuine miss on this sample, precision is a genuine pass. Real c-curl example: tests/libtest/*.c -> tests/libtest/Makefile.inc 157/159; lib/*.c -> lib/Makefile.inc 47/47; CMake/*.cmake -> CMakeLists.txt 22/22.
