
## 2026-09-05 22:11 — START: worktree merged claude/grain-agent-tool-b89y0x, branch fix/115-116-119-selection. Baseline propose (subgate cap lifted, YG_BIN set) confirms the hole: leveldb 0 aspects / 12 types, kotlin-datetime 0 aspects / 32 types. Cause located: buildTypes renders a _root partition as a dir-less rootGlob type (dir: null, id slug(partition)), and buildAspects' typeForPartition matches only on a.dir === partition name, so every row mined in _root finds no host and is dropped silently.

## 2026-09-05 22:40 — DONE. buildTypes now resolves any partition whose name is not a directory (derived: no tracked file lives under the name — covers _root and the _repo residue alike) to the emitted type that actually holds its files, by counting the overlap; the host lookup reads that only after both existing path clauses fail, so no host the renderer already produced can change. Added typeGlob so a dir-less root type globs '*' instead of building the literal 'null/**'. The evidence line discloses that the partition is a label, which type holds how much of it, and that the scope is the type's. MEASURED (propose, subgate cap lifted, YG_BIN set, aspects by status before -> after): leveldb 0 -> 37 (32 draft / 5 advisory); kotlin-datetime 0 -> 46 (43 draft / 1 enforced / 2 advisory); gin 62 -> 168 (50->145 draft, 12->22 advisory, 0->1 enforced); groovy-spock 211 -> 232 (195->214 draft, 10->12 advisory, enforced 6 unchanged). New test tests/propose-label-partition-host.test.mjs on a real 28-file fixture whose single partition is _root: red before (0 aspects), green after (all hosted by src, disclosure asserted, no glob built from the label).

## 2026-09-05 23:08 — Full suite green with YG_BIN and HORDE_DIR set: 2335 tests / 2335 pass / 0 fail (18 more than the 2317 baseline — the four new test files). Yggdrasil proposal diff before -> after, by file class: ADDED 280 (110 drill cases, 96 content.md, 23 CORPUS.md, 23 provenance.json, 23 yg-aspect.yaml, 5 check.mjs) — the 23 new aspects are 119's newly-hosted label-partition rows and the content.md/check.mjs split follows 115; REMOVED 79 (78 check.mjs, 1 drill case) — the absence rows that no longer render a deterministic check; CHANGED 915 (537 provenance.json — every one gains the additive direction field, plus the status/draftReason moves; 186 yg-aspect.yaml — status, absence wording, marker exemption and the label-hosting disclosure; 106 content.md; 72 charter.md — newly hosted aspects reach their node; 6 check.mjs — the filenameshape template's marker guard; 2 CORPUS.md; and one each of yg-architecture.yaml, PROPOSAL.md, REFACTOR-BACKLOG.md, alternatives.md, proposal.json, sizing.json). One honest caveat: latticeRows moved 6552 -> 6553 and subGate 587 -> 588 between the two Yggdrasil runs, which is the export's own time-relative history weighting an hour apart, not the renderer — nothing in these commits touches mining.
