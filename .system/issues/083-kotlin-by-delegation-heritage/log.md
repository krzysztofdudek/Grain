
## 2026-09-02 08:00 — Symptom: Kotlin's class-delegation clause (class Foo(delegate: Bar) : Bar by delegate  /  class X : KSerializer<Y> by someFn()) records the DELEGATE EXPRESSION after 'by' (a constructor parameter name or a function-call name) as a second auto.extends-shaped supertype claim, alongside the real interface name. Confirmed: okhttp ThrottledSink.kt:26 'class ThrottledSink(...) : Sink by delegate' -> claims 'delegate' as a supertype (audit-claims.mjs's own heuristic already recognizes it matches the sec049 constructor/parameter-name shape, printed in its detail string); okhttp JavaNetCookieJar.kt:29 same shape. kotlin-datetime: 10 instances across 5 files in core/common/src/serializers/*.kt, all 'by asKSerializer()' (an extension-function call used as the delegate) -> claims 'asKSerializer' as a supertype, which is a FUNCTION, never a type, and is correctly rejected as fabricated by the auditor's type-shape heuristic (lowercase, not PascalCase). This is the SAME failure class as already-fixed 049 (extends Type(args) records the constructor ARGUMENT as the supertype) but a DIFFERENT clause shape: 049's fix (argRe field-driven predicate, argument_list descent) does not cover Kotlin's explicit_delegation ('by' clause) node, which is a distinct grammar construct from a call's argument_list. 049's own acceptance note named Kotlin as a language to check for the ORIGINAL argument_list shape; this by-delegation shape is a related but separate gap that fix apparently did not reach (13 instances across 2 independent Kotlin repos, cross-checked: 0 such fabrications in the other 21 corpus repos of other grammars). Repro: node tests/stress/audit-claims.mjs <okhttp-or-kotlin-datetime-repo> --json-out out.json; byType.heritageTargetReal. Suspected area: core.mjs heritage-marker extraction's handling of Kotlin's explicit_delegation node (or whatever node-types.json calls the 'by' clause) — should record only the interface/type name preceding 'by', never the delegate expression after it. Acceptance: class Foo : Bar by expr records exactly one supertype claim (Bar); no claim naming the delegate expression or any identifier inside it.
