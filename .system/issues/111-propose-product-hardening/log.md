
## 2026-09-05 19:54 — start: branch fix/111-hardening from claude/grain-agent-tool-b89y0x (3d249bf). Three passes: adversarial correctness, security, simplification with byte-identity gate.

## 2026-09-05 20:32 — pass 1 (adversarial correctness) done — 6 bugs, 6 commits, each with a failing test first: out-dir symlink defeats the refuse-to-overwrite guard and deletes the live .yggdrasil/; a backslash in a tracked path is folded to a separator and fabricates a directory; a newline in a repo path injects YAML through the evidence comment; a control character in a path makes the whole graph unreadable to a conforming parser; no timeout on yg drill (bound derived from the slowest measured drill x100, disclosed when it fires); the spawned export is left in the committable half of .grain/.

## 2026-09-05 20:38 — pass 2 (security) done — 1 commit. Threat: adopter clones an unaudited repo and runs one command; the repo is the attacker-controlled input, the out-dir the sink. The drill corpus is the only place repo bytes are copied wholesale and the only place a repo-derived string becomes several path components of a write. Two findings, both fixed with a real hostile git fixture: a tracked symlink handed the proposal the content of a file outside the repository, and a site path resolving outside the repository was trusted. Verified clean: every spawn passes an argv array (no shell), aspect ids are slug-built so they can never be read as a flag, and the check.mjs code generator interpolates only closed enums and JSON.stringify'd identifiers. Byte-identity gate after passes 1-2: diff -r on the .yggdrasil tree from ygg-export.json is empty and proposal.json is byte-identical.

## 2026-09-05 20:49 — pass 3 (simplification) done — 2 code commits + docs, byte-identity gate passed on each (diff -r over the .yggdrasil tree from ygg-export.json empty; proposal.json, PROPOSAL.md, REFACTOR-BACKLOG.md, alternatives.md, sizing.json all byte-identical). (a) Five parameters nothing reads removed — four were already marked dead in the source by a void statement, the fifth is residue of the retired --min-type-files knob. (b) propose() split at the four seams its own comments name (architecture / nodes / aspects+drills / charters) plus input loading: 243 lines to 110; ev passed in rather than closed over. renderCheck (169 lines) deliberately not split — it is a flat switch of self-contained templates. No duplicated helpers found: jaccard/intersectSize already come from yggdrasil-graph.mjs; core.mjs's pct is a DIFFERENT function (returns a number and floors at 99 so 100% means exactly 1.0) and propose's rounds to a string — latent divergence, not observed on either baseline, left for a ruling since changing it would change rendered output.

## 2026-09-05 20:51 — gates: re-render into an EXISTING out-dir measured (render from ygg-export, then re-render the same dir from a materially different export with half the conventions and partitions dropped, and diff against a fresh render of that same export): diff -r empty, exit 0 — no stale aspects, no stale documents, the rmSync of .yggdrasil plus unconditional rewrites of the five top-level documents already cover it. Self-propose on this worktree after all commits: enforced 0 / advisory 22 / draft 59 (81 aspects, 22 drilled), identical to the pre-work baseline. The alternatives count moved 190 -> 192 because the repository itself grew by the two test files this ticket adds (1470 -> 1472 tracked files); the delta rows all sit in the plugins/grain/tests role groups.
