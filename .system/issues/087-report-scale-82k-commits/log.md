
## 2026-09-02 08:21 — From the ladder: with 055 fixed, symfony-full completes all 11 commands loudly. But report = 38.6 min wall / 5.2 GB peak; cold build 31.7 min; every other command < 12 s. The cost is in the history-derived tables at learn() time (cochange, scopeCochange, changeArchetypes, msgAffinity, obligations) over 82,946 footprints — establish WHICH by profiling learn() on symfony-full (a flame of the derived-table builders), and whether any is super-linear in commits. Candidates without a new constant: incremental derivation (reuse the previous store's tables and fold only new commits — HIST_V already versions the walk), or capping the walk by the existing fpsCap (20000) which symfony exceeds 4×. Do not silently cap without disclosing 'history considered: last N commits' in the output — that is a class D requirement. Instrument: the ladder row itself (completed + wall + RSS) is the acceptance.

## 2026-09-02 11:59 — Profiled, not guessed — the ticket's premise is wrong. Re-cloned the exact pin (82,946 commits / 14,887 files) and ran a node --cpu-prof cold build. report --top 40 on a warm store is 0.46 s / 352 MB, NOT 38.6 min; map right after is 0.39 s. cmdReport is a pure model renderer (no loadHistory, no learn), and after a completed refresh both report and map are plain cache hits — verified on a synthetic repo too. The 38.6 min row can only have been report re-doing a full build after the cold-build step left an unusable cache (readJson swallows every error and returns null -> ensureFresh sees model=null -> full rebuild; grain.mjs:210). The real cost is the cold build: 1986.6 s (33.1 min), 5.15 GB peak RSS, matching the ticket's 5.2 GB — which was the BUILD's RSS, not report's. Attribution of all five named suspects, inclusive of a 1986 s build: buildObligationTable (073) 0.1 s = 0.00%; currentPathOf 0.0 s = 0.00%; toH (cochange+scopeCochange) 1.0 s = 0.05%; replay (msgAff/pairSup/scopePairSup) 10.5 s = 0.53%; induceClusters (changeArchetypes) 6.7 s = 0.34%; learn() ENTIRE 71.2 s = 3.58%. None is super-linear in commit count: induceClusters is flat at 169/171/168/190 ms for 1k/5k/10k/20k items (NCAP=700 bounds it), and cochange finalization is strictly linear at ~1.2 us and ~430 B per distinct pair. The derived tables DO respect fpsCap — they read H.fps, capped to 20,000 at history.mjs:771 before toH runs. parseBlobs is 76.14% (1512.4 s). The one genuine super-linearity is in BlobCache: parseBlobs flushes once per 400-blob batch and flush() evicts every shard it writes, so the next batch re-reads, re-parses, re-serializes and rewrites the ENTIRE cache — 528 times. Measured: shard 296.8 s incl (14.9%, 227.2 s self in JSON.parse) + flush 233.7 s incl (11.8%, 156.5 s self in JSON.stringify) = 530 s = 26.7% of the build moving bytes just written. Removing the eviction is NOT the fix: tried it, and symfony finished all 211,065 blobs then died in replay with 'Ineffective mark-compacts near heap limit' at a 4.08 GB heap — the eviction is the memory bound. Fixed instead by widening the shard key 2->3 hex (256 -> 4096 shards), so a 400-blob batch dirties ~390 shards holding ~1/4096 of the cache each instead of 242 of 256 = the whole thing; eviction and memory behaviour unchanged. Not a new constant and not a cap: nothing is truncated, so no class-D disclosure is triggered. Red->green: tests/history-blobcache.test.mjs (a) fails at width 2 ('got 242') and passes at 3, five companion tests hold at both widths. Suite 2166 pass / 0 fail. NOT DONE / for the director: capping the WALK at fpsCap would cut distinct code blobs 239,212 -> 131,625 (-45%, measured; git log --reverse -n N verified to keep the newest N) and is the only lever on the remaining ~980 s of genuine tree-sitter parsing — but it truncates every large repo's history, needs the 'history considered: last N commits' disclosure on freshnessLines, and would need a HIST_V bump. Separately, model.obligations' rules/ambient arrays and the pairSup/scopePairSup/msgAff/lc/vev accumulators are uncapped and grow with full history (RSS, not CPU).
