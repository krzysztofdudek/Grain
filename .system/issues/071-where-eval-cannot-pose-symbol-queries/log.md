
## 2026-09-02 03:25 — Measured ceiling for symbol-first on the named stratum: exactly 0 across 12 repos — no miss has a query word verbatim equal to a declared name, because whereEval builds queries from commit messages through tokenize+normTok (camelCase split, stemmed). The machinery (where sendStatus → lib/response.js at 100%) works for a human query; the harness cannot type one. This is an INSTRUMENT boundary: the harness under-measures a real capability. Options to establish: (a) a second query mode that preserves identifier-shaped tokens from the commit message (any token with an inner capital or underscore kept verbatim alongside its split form); (b) a separate 'symbol stratum' in selftest --where reporting hit@3 for candidates whose message contains an identifier-shaped token. Either must not change the existing strata's numbers (they are the comparison baseline). No engine ranking change in this ticket.

## 2026-09-02 04:27 — Fixed: added fp.symToks (verbatim identifier-shaped words from the raw commit message, history.mjs) and an additive 'symbol' stratum in whereEval (core.mjs) + selftest --where CLI report line; option (a) chosen. Pooled/named/unnamed strata numbers verified bit-for-bit unchanged (full suite green, exact-value assertions in where-eval.test.mjs untouched). Measured on grain's own repo: symbol n=3, hit3=0.667 vs pooled n=38, hit3=0.842.
