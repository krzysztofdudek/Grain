# 071 · selftest --where cannot reward a symbol-first path: queries come from commit messages via tokenize+normTok, so 'sendStatus' becomes send+status and never matches a declaration

**Status:** FIXED — added additive symbol stratum to selftest --where (verbatim identifier tokens preserved alongside split/stemmed); old strata numbers proven bit-for-bit unchanged
**Found by:** design-where-named (Opus), 2026-09-02
**Severity:** medium
**Class:** E

## Symptom

## Suspected area

## What is NOT in scope

## Acceptance
