
## 2026-09-05 23:18 — Start: worktree on research/110-type-levels, merged agent-tool branch (116 path: predicates present). Reading buildTypes + oracles-4 measurement.

## 2026-09-05 23:30 — Policy sweep done (4 oracles, 14 intrinsic-only policies, scored at J>=0.5). Ceiling analysis: today 15/2/6/21 hand types; every-directory ceiling 22/11/7/24; grain's own published levels only reach 17/4/6/21 — the missing level is the DIRECTORY OF FILES GRAIN PARSED NONE OF (resources, templates, fixtures, docs), invisible because a directory card is published only where scopes were mined. Winning policy 'unmined OR purer' (a finer directory becomes a type only where it beats the level above on that level's own evidence: strictly more of its imports stay inside, or grain read none of its files while it read the parent's): recall 16/33, 6/28, 7/13, 23/36 (was 15,2,6,21) — wins on all four, loses on none; precision 22/36, 7/16, 8/29, 25/107 (was 18/31, 3/12, 7/22, 23/82). Ceiling policy 'every directory' refused: 418 types on Yggdrasil.

## 2026-09-05 23:56 — Implemented and measured end to end. TYPE_LEVELS = partition|module|directory|domain|role group|layout; typeEvidence() gives every candidate the same oracle-free numbers (files, imports inside/crossing, co-change inside/crossing, modal file-name shape, files grain parsed, mined rules whose every site is inside). Derived policy shipped: a finer directory becomes a type only where it beats the level above on that level's own evidence (strictly more imports stay inside, or grain read none of its files while it read the parent's) — the ONLY intrinsic-only policy of 14 that wins on all four oracles and loses on none. Type recall 15->16/33 grain, 2->6/28 petclinic, 6->7/13 express, 21->23/36 Yggdrasil; with alternatives 21->22, 6->9, 6->7, 23->25. Node recall also up: 15->16/35, 0->2/20, 6->7/15, 30->43/393. Active types 31->35, 12->16, 22->29, 82->106. yg check on staged copies: petclinic PASS 18 nodes, grain PASS 37 nodes 1466/1470, express FAIL (same 80 unverified-aspect errors as before), Yggdrasil FAIL (same class, 85 nodes, 1366/1615 85%) — no graph-loading error anywhere, 4/4 load. Horde node.mjs show reads the new charters. Alternatives now grouped by level in alternatives.md and carried as kind:alternative rows in proposal.json.

## 2026-09-06 00:13 — Committed as 56e4c5e on research/110-type-levels. npm test with YG_BIN + HORDE_DIR + GRAIN_CORPUS_CLONES: 2382 tests / 2382 pass / 0 fail. One documentation bug fixed on sight: section 4's header claimed active types form an ANTICHAIN — they never did, the family is LAMINAR and that is what makes deepest-wins a function; the sentence was corrected, not the behaviour.
