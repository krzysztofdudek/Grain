
## 2026-09-02 08:00 — Symptom: a Rust trait-bound list carrying the 'static lifetime (e.g. 'pub trait Listener: ... + Send + Sync + 'static' or a trait object 'dyn Handler + Clone + Send + Sync + 'static') records 'static as its own auto.extends/implements-shaped heritage claim, alongside the real trait names. 5/29 (17%) of axum-full's checkable heritageTargetReal claims are exactly this: axum/src/serve/listener.rs:16 Listener, connect_info.rs:74 Connected, serve/mod.rs:162 Executor, extract/ws.rs:416 OnFailedUpgrade, handler/mod.rs:148 Handler — all 'extends/implements 'static'. 'static is a LIFETIME ANNOTATION, never a trait or type, so this is a fabricated fact by construction (same class as macroTokenAsName/argument_list-in-heritage — a heritage-vocabulary node matching something in the clause that is not actually the supertype/trait being named). serde-full (also Rust, in this same corpus wave) shows 0 heritage fabrications, so the pattern is specifically tied to axum's heavier use of 'static bounds in trait-bound lists (handler/extractor generic bounds), not universal to all Rust code, but it is reproducible and not a one-off. Repro: node tests/stress/audit-claims.mjs <axum-full-repo> --json-out out.json; byType.heritageTargetReal (5/29). Suspected area: core.mjs's heritageRe / heritage-marker walk for Rust's trait_bounds or bounds node — a lifetime node ('static, or any 'tick-prefixed identifier) should never be treated as a candidate supertype name; likely needs an exclusion parallel to the existing keyword/argument_list filtering (node-types.json-derived: any grammar's lifetime-shaped node type, not a Rust-only rule if this generalizes). Acceptance: a trait bound list containing 'static records supertype claims only for the real trait names in the same list; 'static itself is never recorded as a target.

## 2026-09-02 10:16 — Rebased/re-landed the 'static lifetime heritage fix onto 082's heritageNamesOf refactor and 083's inDelegate rebase — not new work: added lifetimeRe (bindingFor) and an inLifetime ancestor-walk flag alongside inArg/inPrefix/inDelegate, ported heritage-lifetime-bound.test.mjs (7 tests) unchanged; 082's dotted-heritage and 083's heritage-kotlin-delegation tests confirmed unaffected (12/12); full suite 2141->2148, 0 fail. config.mjs EXTR_V bump still deferred to next batched round (084's original report already flagged this).
