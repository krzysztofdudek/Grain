
## 2026-09-02 07:59 — Symptom: class Foo(pkg.sub.Type): emits THREE separate auto.extends-shaped supertype claims — one for 'pkg', one for 'pkg.sub', one for 'pkg.sub.Type' — instead of resolving to the single real base 'Type' (which IS declared locally in most cases, e.g. Flask's own Flask class). Confirmed on flask/flask (heritageTargetReal 78/158 = 49.4% fabricated, ALL of them this shape: tests/test_views.py:201 'class BaseView(flask.views.MethodView):' alone produces claims against 'flask', 'flask.views' AND 'flask.views.MethodView', none of which match declaredTypeNames or importTargets even though bare 'MethodView'/'Flask' would). Not the same as 062 (qualified heritage takes namespace, not member — already fixed, 8 grammars) or 049 (argument_list constructor-call in extends — already fixed): those cover a SINGLE mis-resolution per clause; this is python's dotted attribute node emitting MULTIPLE overlapping claims per clause, one per nesting depth, none collapsed to the leaf. Checked other grammars in this wave's corpus run (C#, Java, Go, Groovy, JS/TS, PHP, Ruby) — zero heritage fabrication of this dotted-prefix-explosion shape; appears Python-specific (or at least uniquely surfaced by tree-sitter-python's attribute node nesting under class_definition.superclasses). Repro: clone flask@d318b68, node tests/stress/audit-claims.mjs <repo> --json-out out.json, inspect byType.heritageTargetReal (78/158). Suspected area: core.mjs heritage-marker extraction walking every nested attribute/dotted_name descendant as its own name-shaped candidate instead of taking only the outermost clause's resolved leaf, for Python's specific superclasses argument_list->attribute shape. Acceptance: class Foo(pkg.sub.Type) records exactly one supertype claim, resolving to Type (or the fully-qualified name if Type is genuinely external and unresolvable) — never 'pkg' or 'pkg.sub' alone as their own claims. Per-language diff showing no other grammar's heritage output changes.

## 2026-09-02 08:40 — fixed: Python's superclasses field now shares the §062 leaf-only qualName resolution (heritageNamesOf) instead of its own naive descendantsOfType(identifier+attribute) walk; flask heritageTargetReal fabrication 78/158 (49.4%) -> 0/63 (0%), suite 2129->2135 pass, 0 fail
