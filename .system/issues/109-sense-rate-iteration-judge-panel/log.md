
## 2026-09-05 19:57 — Start: worktree fast-forwarded to wave-9 base (9564810), branch research/109-sense. Reading 101 stress report, rulings, and propose.mjs wording templates before any rendering.

## 2026-09-05 20:14 — Coin recorded (sha256 salt 'grain-109-sense-split:', hex ascending, first 9 DEV): DEV = cpp-json tsx-zustand serde-full telescope.nvim axum-full groovy-spock sinatra leveldb spring-petclinic; HOLD-OUT = flask kotlin-datetime gin Slim zig-zls bash-it express openzeppelin-contracts CleanArchitecture. Judging protocol frozen before any verdict (PROTOCOL.md in scratch): 3 passes, different orderings, card shows status+sentence+evidence only, measures = all-three agreement on (a), majority sense (a AND c), Cohen kappa per pass pair. Disclosure written in first: the panel is ONE model in three passes, not three agents. cpp-json excluded from the corpus for the same documented reason ticket 101 recorded — its grain export history walk does not finish in a usable budget (12146 blobs, ~800 in 25 min under load); DEV is therefore 8 repositories, HOLD-OUT 9, and the coin is unchanged.

## 2026-09-05 20:27 — Renderer changes developed and validated in a scratch copy while the baseline rendered. Two things found on sight and fixed with red-green tests: (1) a lattice row was worded from its internal predicate id instead of from the value it was measured at, so every categorical family produced a rule naming nothing - "follow the name shape" with an empty identifier, and "files in themes have auto.lex:quote", an internal id printed at a maintainer; 25 of the first 250 rendered aspects, six of them already promoted to advisory by a real drill of a check that WAS reading the right value. (2) plural and article agreement in the type/charter evidence ("these 1 file", "a assignment_expression", and "the rest belong to a nested node" on a node that owns everything it maps). A separate finding for its own ticket, NOT touched here because it is attachment and not wording: a partition named _root has no host type since 101's root-glob fix, so every rule mined inside it is dropped - kotlin-datetime and leveldb now propose ZERO aspects (251 and 134 mined files, 9 certified conventions on leveldb alone), and gin and groovy-spock lose their _root partitions' rules too. 4 of 11 measured repos affected.

## 2026-09-05 20:38 — Baseline render of the corpus at the SHIPPED defaults (SUBGATE_PER_PARTITION not lifted, unlike 101 - 109 measures the wording an adopter actually receives). Verified rather than assumed, because it decided the schedule: grain export --no-history is NOT a safe shortcut for a slow repository. On spring-petclinic it changes what is mined - 18 aspects become 12, 12 rendered checks become 9, drill cases 82 to 50 - so every repository here is exported with the full history walk however long it takes. cpp-json stays out for the reason 101 already recorded. First commit landed: the lattice-row pid/value defect, red-green.

## 2026-09-05 20:58 — BASELINE JUDGED on DEV, 82 items, three passes. Result: rule (a) = 0 of 82, in every pass, in every category. Not one baseline sentence reads as a rule an agent can obey or violate; sense (a AND c) = 0.000. Because (a) has no variance at baseline, Cohen's kappa is UNDEFINED there (p_e = 1) and only raw agreement can be reported: 1.000 on all three pass pairs. Judge reliability therefore cannot be claimed from the baseline round; it is measurable only once (a) starts varying. Truth (b) is where the baseline separates: 1.000 on certified conventions but 0.053 on sub-gate lattice rows and 0.031 on the advisory status - i.e. of the rows the quiet report actually shows a maintainer, 31 of 32 were judged untrue as worded, because they state a majority (70-87%) as if it were a rule. Two defects found while judging and fixed in the round-1 commit: a decorator argument that already carried its sigil rendered as @@Override, and plural/article agreement. Round 1 committed (c8ee591) with all three propose suites green; DEV re-render in flight.

## 2026-09-05 21:06 — ROUND 1 JUDGED on DEV, same 82 items, three passes. rule (a) 0.000 -> 0.866. SENSE (a AND c) 0.000 -> 0.293. truth (b) 0.500 -> 0.598. All three passes agreed on (a) for all 82 items again, and now that (a) actually varies (71 yes / 11 no) Cohen's kappa is defined: 1.000 on all three pass pairs. Wording-only proven by full-tree diff across all 8 repos, and yg drill output byte-identical before and after on spring-petclinic, tsx-zustand and telescope.nvim (22 aspects). Where it did NOT move: enforced aspects are 1.000 on (a) and 0.000 on sense - 12 of 12 failed (c) for one reason the wording now makes visible rather than hides, that `enforced` is earned by a drill proving the CHECK correct and never by the repository already holding the rule, so every enforced item blocks between 1 and 18 files that exist today. And types are 0.167 on (a): a type with no relations and no rules attached obliges nothing at all, whatever the sentence says. Round 2 will target the three things the free text actually named that ARE wording: root-glob nodes printing `null/`, a miner file count printed beside a different glob selection count so the two contradict, and the type sentence stating placement passively.

## 2026-09-05 21:22 — HOLD-OUT RESULT, nine repositories the wording was never tuned on, 69 items, judged twice. rule (a) 0.000 -> 0.928. SENSE (a AND c) 0.000 -> 0.493. truth (b) 0.551 -> 0.710. It generalises better than it fits: DEV, where the wording was tuned, ended at 0.305 sense; the hold-out at 0.493. Charters 0.000 -> 1.000, types 0.000 -> 0.750, aspects 0.000 -> 0.289. Round 2 moved DEV sense by one item of 82 so the frozen stopping rule ended the iteration at two rounds. Judge reliability, stated honestly: three passes agreed on (a) for every item in every round, but on the two baseline rounds kappa is UNDEFINED because (a) has no variance, and on the rounds where it is defined it is 1.000 for a reason that flatters nobody - once the obligation form exists, "does the sentence tell the agent what to do" is close to mechanical. The judgement carrying real information is (c), and that is one model's opinion three times. yg drill transcripts byte-identical before and after on 22 aspects across three repos; yg check loads 17 of 17 with zero load-blocking codes.

## 2026-09-05 21:34 — Done. Branch research/109-sense, four commits: db38a84 (defect: a lattice row was worded from its internal predicate id instead of the value it was measured at, red-green), c8ee591 (round 1: the obligation form, prohibitions, evidence line leading with holds/breaks, role cluster out of the sentence, types and nodes saying what they oblige, plus the @@Override sigil and plural/article defects), 5343fea (round 2: root-glob nodes naming the repository root instead of null/, the miner file count said as a subset of the glob selection, the type sentence as an instruction), cd472df (.system/research/sense-iteration.md). npm test in plugins/grain with YG_BIN and HORDE_DIR: 2293 tests, 2293 pass, 0 fail. docs/reference.md gained a "How a proposed rule is worded" section; README quotes no propose sentences so nothing there needed changing. Nothing pushed.
