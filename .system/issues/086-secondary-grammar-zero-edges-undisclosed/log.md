
## 2026-09-02 08:10 — instrument A's resolutionCoverage check (audit-claims.mjs) measured, across this wave's corpus run, a specific sub-shape distinct from the already-fixed 041/059 class (which was about a WHOLE grammar, e.g. all of C/C++ or all of PHP cross-component, having no edges at all): a repo dominated by one grammar has a SECOND, smaller grammar population whose files carry ZERO in/out edges anywhere, and the report's coverage-disclosure line never names that grammar (only genuinely no-grammar file extensions get named). Counts this wave (files with zero edges / grammar / repo): okhttp 71 java (100% of okhttp's .java population per corpus.json's own count of 71 total — this is the corpus's dedicated 'mixed-source-sets' axis repo, Kotlin+Java); playframework 24 javascript (Play apps' asset-pipeline JS; also a dedicated mixed-source-sets + heritage-constructor-args axis repo, Java+Scala); groovy-spock 5 kotlin (dedicated mixed-source-sets axis repo, Java+Groovy — its own small Kotlin population); cpp-json 7 python (a C++ repo's small helper-script population); kotlin-datetime 4 java (a near-pure-Kotlin repo's tiny Java population); axum-full 5 javascript (a Rust repo's doc/example JS snippets). Six repos, four distinct grammar pairs, ALL THREE of the corpus's dedicated 'mixed-source-sets' axis repos (okhttp, playframework, groovy-spock) show it — this directly undermines the axis those three repos were pinned to validate. Severity scales with population size (71/24 files is not an edge case) but the SHAPE (silently zero, never disclosed) is uniform regardless of population size. Repro: node tests/stress/audit-claims.mjs <okhttp-or-playframework-repo> --json-out out.json; byType.resolutionCoverage samples matching /have zero in\/out edges/. Suspected area: whatever in core.mjs/relations.mjs decides which grammars get an edge resolver wired up per-repo — likely only the DOMINANT grammar (by file count, or the one .grain infers as 'primary') gets full import/use resolution, with a secondary grammar's files silently falling back to zero edges rather than a real (even if partial) resolver attempt; separately, relCoverageData's own disclosed-list computation should include ANY grammar meeting the coverageMin population floor with literally zero edges, not just ones some other code path already flags. Acceptance: a mixed-source-set repo either resolves the secondary grammar's own edges (imports within itself, at minimum) or the coverage note names it exactly like it already names C/C++ (041) and PHP (059).
