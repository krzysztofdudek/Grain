
## 2026-09-02 08:54 — Spec from 081's research doc + escalation 12. Facts: 63 of 63 grain CLI calls across 36 agent runs went to a command named in sessionContext's advertisement (61) or SKILL frontmatter (2); 0 to any of the 12 unadvertised commands, obligation included. Therefore: (a) rewrite the SessionStart advertisement so each command is named WITH its trigger moment, and obligation/completeness appear at 'before creating a file' / 'before you consider the change done' — keep it short; the catalog says agents read the first lines; (b) the PreToolUse hook that already fires before Write of a NEW path should run obligation <path> and print its two labelled sets when the birth rule certifies (bounded output; measure the fire rate on clean new files — 037 standard; silence when nothing certifies, never a hollow line); (c) the SKILL frontmatter description names the obligation family. JUDGE on the trial harness (.temp/stress/h040/): grain invocations by command, and answer-changed-diff — the only metric that matters. No engine change to ranking; this is surface + hook.

## 2026-09-02 10:49 — SessionStart advertisement now names obligation (at where's 'before creating a source file' trigger) and completeness (at check's new 'before you consider the change done' trigger) as asides on the existing where/check lines, not new slots -- 5 unconditional lines unchanged, 2140->2309 chars (+7.9%, worktree path inflates all Run: strings); check-hook --pre now also emits obligation's two labelled sets, gated strictly on rules.length (specific certification), never on ambient-only or n=0 -- measured 0% fire rate on this repo's own history (selftest --obligation: n=97, coverage 0.00) and 0/8 on ticket 081's real trial creation events, so the hook stays silent on ordinary creations by construction; SKILL.md frontmatter description now names obligation and completeness too. 4 new tests (obligation-hook.test.mjs x3 fires/silent/below-minRaw, grain.test.mjs section-088 trigger-moment phrasing), full suite 2164/2164 pass (was 2160), no config.mjs or version touch.

## 2026-09-02 13:01 — TRIAL B FOLLOW-UP (trial-0.4.0-b): the advertisement worked (obligation 0→4 calls, breadth 3→6 commands) but 3 of the 4 obligation calls asked about a file that ALREADY EXISTED and was about to be edited — the ad folds the birth question into the where line and agents generalise it to 'the file I am about to touch'. Small wording fix: the obligation line must say 'before CREATING a file that does not exist yet' and the where line must not imply obligation applies to edits. Also: post-088 agents chain several grain calls in one Bash command; the trial extractor was fixed to count them — any future transcript tooling must split chained calls. No engine change.
