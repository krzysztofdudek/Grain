
## 2026-09-02 08:54 — Spec from 081's research doc + escalation 12. Facts: 63 of 63 grain CLI calls across 36 agent runs went to a command named in sessionContext's advertisement (61) or SKILL frontmatter (2); 0 to any of the 12 unadvertised commands, obligation included. Therefore: (a) rewrite the SessionStart advertisement so each command is named WITH its trigger moment, and obligation/completeness appear at 'before creating a file' / 'before you consider the change done' — keep it short; the catalog says agents read the first lines; (b) the PreToolUse hook that already fires before Write of a NEW path should run obligation <path> and print its two labelled sets when the birth rule certifies (bounded output; measure the fire rate on clean new files — 037 standard; silence when nothing certifies, never a hollow line); (c) the SKILL frontmatter description names the obligation family. JUDGE on the trial harness (.temp/stress/h040/): grain invocations by command, and answer-changed-diff — the only metric that matters. No engine change to ranking; this is surface + hook.
