
## 2026-09-02 05:31 — Precision is not the problem (0.958 macro / 1.000 non-obvious); coverage is. Establish, each in isolation with the other held fixed, on the same 7 repos: (a) harness minimum births — if selftest --obligation requires ≥6 births to fire while CFG.minRaw is 5, the harness under-reports the rule; align to the engine's own floor and re-measure; (b) key granularity — the design doc §3 found finer (directory-path, suffix) keys win, but §6 mandated (refined module, suffix), which collapses paths deeper than two segments; measure coverage AND precision under a (leaf directory, suffix) key and under the current key. Accept a change only if coverage rises toward 0.08 with macro precision staying ≥0.80 — a coverage gain bought with precision is rejected. No new constant: the key is a choice of existing derivation (refineModOf vs dirname), the floor is CFG.minRaw.

## 2026-09-02 06:09 — BOTH HYPOTHESES REFUTED — ship neither; 0.048 coverage stands as measured. Same 7 repos, same instrument, `selftest --obligation --last 300`, each hypothesis in isolation with the other held fixed. BASELINE REPRODUCED EXACTLY (validates the setup): n=1822, 88 fired, coverage 0.0483, pooled p@1 0.9773, repo-macro p@1 0.9583 (3 of 7 firing), non-obvious 70/70 = 1.000, hot-null 0.193 — every figure identical to 073.

(a) HARNESS MINIMUM-BIRTHS — REFUTED, no instrument bug. The harness does NOT require 6 strictly-prior births; it requires exactly 5 = CFG.minRaw, the engine's own floor. Synthetic probe: with 10 births of one class the harness fires on 5 of them, so the 6th birth is the first that may fire (5 strictly prior). Engine side via buildObligationTable: certifies at 5 prior births, silent at 4. The two agree exactly — nothing to align, and lowering the floor would be a new constant, which this ticket forbids. Counterfactual measured anyway to price the mechanism the hypothesis blamed: a floor of 4 moves coverage 0.0483 -> 0.0510 (macro p@1 0.9706, 4 of 7 firing). Even that forbidden loosening buys +0.003 and lands 0.03 short of the 0.08 bar, so the support floor is not where the coverage shortfall lives.

(b) KEY GRANULARITY, (leaf directory, suffix) via dirname vs the shipped (refined module, suffix) via refineModOf — REFUTED on the coverage bar. Coverage FALLS: 0.0426 vs 0.0483 baseline, moving AWAY from 0.08; repos firing drops 3 -> 2. Precision rises (pooled p@1 1.000, repo-macro 1.000, non-obvious 71/71) but the gate requires coverage to RISE with precision held above 0.80, and this is the mirror of the rejected trade — a precision gain bought with coverage. Per repo: c-curl 0.2367 -> 0.2433 (the only gain, p@1 1.000 both ways); cpp-json 0.0533 -> 0.0167 (16 fired -> 5, p@1 0.875 -> 1.000); flask 0.0033 -> 0.0000 (loses its single firing event); express, sinatra, gin, petclinic 0 under both.

MECHANISM (why the design doc §3 finding does not transfer): the finer key splits the same births across 7-47% more classes, so FEWER clear the CFG.minRaw support floor. Births living inside floor-clearing classes — the population that can ever be answered — fall in 6 of 7 repos: petclinic 53 -> 7, cpp-json 333 -> 285, express 234 -> 199, flask 146 -> 132, c-curl 1588 -> 1581, sinatra 226 -> 218, gin 80 -> 80. §3 measured a single 80/20 split where every scored event shared the full training set, so a deep class could bank support it had not yet earned; under 073 prospective per-event protocol the same fine key starves classes of prior support. Both findings are correct about their own protocol.

SHIPPED: nothing that changes behaviour. No MODEL_V bump needed — the key derivation is unchanged. One test added (plugins/grain/tests/selftest-obligation.test.mjs) pinning the SPEAKING side of the support-floor boundary, which no existing test covered: the leak guard pins only the silent side (4 prior births certify nothing). Fixture with 8 births of one class fires on exactly 3 (births 6, 7, 8); injecting hypothesis (a) alleged bug — one extra prior birth demanded in obligationEval alone — drops it to 2 and turns the test red, and no pre-existing test caught that drift. Full suite 2116/2116 pass, 0 fail (baseline 2115 + this one). Commit 3ad066e on research/078.
