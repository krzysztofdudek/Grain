
## 2026-09-05 22:11 — START: branch fix/115-116-119-selection. Three parts: (1) domain alternatives emitted with a path: glob predicate derived from the members' common directory prefix, file list only as fallback, form named in the evidence line; (2) filenameshape exempts language marker files whose name the language spec fixes, list hung off the per-language grammar bindings in engine/config.mjs; (3) .grain/ leak. Leak path FOUND: propose's own gitFiles() takes git ls-files verbatim and never applies grain's HARD_EXCL, so a repository that commits .grain/.gitignore + .grain/seeds.jsonl (the two files grain itself declares committable) gets a '.grain' uncovered TYPE at GROUP_MIN=2.

## 2026-09-05 22:59 — DONE, three parts, three commits. (1) DOMAIN CUT AS A PREDICATE: buildTypes now offers a finer candidate as a path: glob over the deepest directory all its members share, whenever that directory is below the host; the any_of file list survives only where the members share no such directory, and each candidate's evidence line names the form and the reason (the directory in one case, its absence in the other). Driven end to end in tests/propose-domain-cut-predicate.test.mjs: the candidate is activated in a staged copy as a type plus a node, a Java file grain has never seen is added under the domain, yg check loads clean and yg context --file resolves it to the new type. (2) LANGUAGE MARKER FILES: engine/config.mjs gains LANG_MARKER_STEMS beside the extension map, keyed by grammar, each entry naming the part of the language that fixes it — java package-info/module-info (JLS 7.4.1, 7.7), python __init__/__main__, go doc, index for every JS/TS extension, rust mod/lib/main; the extension view is derived from the same map so a name is never exempt in a language that does not fix it. Only filenameshape exempts them: such a file leaves the population before the support floor, leaves the drill corpus, and is skipped by the rendered check, and the evidence line says how many left and which. MEASURED on petclinic: the 5 package-info.java refusals are 0 (staged, promoted, yg check --approve --only-deterministic). On groovy-spock one ENFORCED rule (spock-core-src-main-java/partition-filenameshape) drops to draft/no-catch — its whole violates corpus was 5 package-info.java files, so it had earned enforced entirely on files Java names for it. (3) .grain LEAK PATH FOUND AND CLOSED: propose's own gitFiles() took git ls-files verbatim and never applied grain's HARD_EXCL, so the filter held for what grain MINED and not for what it PROPOSED; a repository that commits .grain/ (which grain's own .grain/.gitignore instructs — 'everything else in .grain/ is meant to be committed') got '.grain' as an uncovered node TYPE. HARD_EXCL now also covers .yggdrasil-proposal/, grain's own staging tree, and propose applies it; walkWorktree skips it too. tests/propose-own-state-never-proposed.test.mjs commits both and asserts neither reaches any type, node, evidence row or rendered document.
