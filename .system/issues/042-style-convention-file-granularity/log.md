
## 2026-09-02 03:14 — Reproduced on telescope.nvim at HEAD: 7 single-quoted literals added to a 100%-double-quoted file, check --json still conforming:1 share:1, zero flags. Granularity established from the code, not the symptom: lexicalPreds (core.mjs ~2030) is a genuine per-FILE ratio statistic, not per-literal-under-a-majority — one categorical per file, 'double' while <=20% are single-quoted. Measured flip points end to end: buffer_previewer.lua silent at 50 added literals, flips at 51; express test/acceptance/mvc.js silent at 12, flips at 15. Budget = 0.25 x majority count, grows with file size: 957 literals repo-wide on telescope.nvim, 2050 express, 1067 flask. General, not Lua-specific (reproduced in JS; :semi/:decl/:indent share the shape). Chose acceptance outcome (b): per-literal mining rejected on evidence — of the literals departing their file's majority, 11/11 telescope, 19/31 flask, 2/24 express contain the majority delimiter in their own body, so the other quote is forced by content, not chosen. Shipped the honest-disclosure fix instead: governed[].withinFile in --json plus a clause on the conformance line, printed even when diff scoping keeps a file-kind fact off it. Counts are an out-parameter of lexicalPreds, never a predicate: acceptance, idxCost and the candidate universe untouched, confirmed by rebuilding both corpora on each engine (telescope 9/9 conventions 1376/1376 scopes, express 26/26 2473/2473, identical). Tests 2011->2016 green; 2 new tests fail on unmodified main. Residue recorded in docs/validation.md: 22 literals on express and 12 on flask depart with no forcing delimiter and are still only counted, never flagged. NO new tuned threshold added; nothing escalated.
