
## 2026-09-06 05:00 — Starting 120: render-side identifier-hygiene guard in engine/propose.mjs buildAspects, per the four classes in .system/research/sense-iteration.md §10 (2)-(5). Surface restricted to propose.mjs (buildAspects + row classification) plus tests/docs; core.mjs/relations untouched (117 owns the split).

## 2026-09-06 05:02 — Class 1 (parser node type as identifier) + class 2 (generic type parameter as domain type) implemented in buildAspects: identifierUnderTest() picks the right value per family (colon-arg for imp/call/deco/extends/has/returns/ptype/stshape, established/expected value for the bare-pid STRUCT_PID families first1/ret/varshape); isParserNodeTypeIdentifier() tokenizes the identifier and checks every token against nodeTypeNamesFor(grammar), built by reading each grammar's own engine/grammars/tree-sitter-<g>.node-types.json (named entries + named subtypes) rather than a hand list, for the grammar(s) the host type's own files are written in (config.mjs EXT2GRAMMAR by extension). Matches: skip entirely, counted in skipped.notARule with skipped.notARuleByReason['parser-node-type-as-identifier']. Class 2: looksLikeGenericTypeParam() applies only to ptype/returns/extends, matches a bare uppercase letter or T<Word> form, and excludes any name found declared as a real type (kind:'type') anywhere in the export's own conventions sites/exemplars or role-group members (declaredTypeNames) -- reason 'generic-type-parameter-as-domain-type'. Extractor gap logged separately (below): RESULT_EXCLUDE in core.mjs explicitly drops type_parameters from the callable surface walk, and neither fileSups nor fileTypeRefs is exported at all, so there is no extracted fact today that would let this be done from real type-parameter facts instead of a form heuristic.

## 2026-09-06 05:02 — Class 3 (cluster measured narrower than the scope a check enforces) implemented for sub-gate rows only (certified conventions already scope by the context they were measured over): clusterScopeFor(r, host) compares the role-group's own deduped member file set against the host type's full population; when narrower, it tries an explicit any_of path list (only when the export's own group.members array is not itself truncated below group.size -- the 200-cap in export.mjs) and falls back to the same contentRegexFor() shared-content predicate a certified group-scoped convention already uses. When neither is exact (cScope.ok === false), the row renders NO check at all -- forced regardless of what the family would otherwise support -- and carries draftReason: 'cluster-narrower-than-scope' so promoteEnforceableAspects can never promote it (its early '!a.check' branch preserves a pre-set draftReason, same mechanism absence-not-forbiddance already relies on). The obligation sentence keeps naming the host's own glob (matching how scopeFor's existing group branch already words certified group-scoped rules) with a 'that belongs to role group ...' or content-predicate qualifier carrying the real scope.

## 2026-09-06 05:20 — Class 4 (types with no law) implemented in propose() (not buildAspects): typesWithNoLaw = active types whose aspectIds is empty (set right after buildAspects runs) AND appear on neither side of any pairs entry from buildRelations() (the same edges writeArchitecture turns into the graph's relations). Still emitted (coverage), disclosed via counts.typesWithNoLaw, a new 'Types with no law' section in PROPOSAL.md, and the on-disk report line. New test file plugins/grain/tests/propose-identifier-hygiene.test.mjs: three REAL git-repo fixtures (a) 36 TS functions where 30/36 first-statements are return_statement -- first1/ret/stshape rows are ALWAYS node-type-valued by construction (core.mjs), confirms class 1 fires with zero artificial naming and class 4 fires on the same repo since every candidate row is dropped; (b) a real function literally named 'identifier' called from 26/32 files -- the exact coincidence the ticket names, confirms class 1 also catches a genuine mined value that merely collides with a node type name, while a sibling non-colliding import row survives untouched; (c) 25 generic identity<T> functions vs 5 concrete-typed ones -- confirms class 2 drops both the returns:T and ptype:T rows (share 0.833, inside the sub-gate band) while leaving PROPOSAL.md's new table row visible. Two buildAspects()-level unit tests (same precedent as propose.test.mjs's own content-predicate/node-charter tests, since engineering a real repo whose MDL role-group clustering lands at an exact chosen cluster size is not practical): class 2's declared-type escape hatch (M kept, V dropped) and all three clusterScopeFor branches for class 3 (content-predicate narrowing, explicit-file-list narrowing, and the no-exact-scope-available case verified end to end: check null, draftReason cluster-narrower-than-scope, skipped.clusterNarrowerThanScope counted). node --test on the new file: 8/8 pass.

## 2026-09-06 05:24 — FINDING for after 117 (extractor gap, not fixed here per the render-side-guard-now instruction): core.mjs's callable-surface walk explicitly excludes 'type_parameters' from RESULT_EXCLUDE, so a declared generic type parameter (<T>, <K, V>) is never recorded as a fact distinguishing it from a real domain type reference -- and neither fileSups nor fileTypeRefs (the two per-file type-hint stores that WOULD carry this) is part of the grain-export/1 schema at all (export.mjs has no fileSups/fileTypeRefs field). Once 117 lands, the real fix is: (a) capture each callable's own declared type-parameter names (from the grammar's type_parameters field) as a scope-level fact, and (b) export it (or a derived exclusion set) so buildAspects's class-2 guard can move from the current form-plus-declared-elsewhere heuristic (CONVENTIONAL_TYPE_PARAM_RE + declaredTypeNames) to an exact per-scope test: 'is this identifier one of THIS callable's own declared type parameters'. The current guard is deliberately conservative (never drops a name that is genuinely declared as a real type anywhere the export's own conventions/role-groups saw) and was verified end to end on a real repo (25 generic identity<T> functions correctly dropped; a declared single-letter class kept, unit-tested in buildAspects directly since the declaredTypeNames census depends on which conventions/groups happened to form).

## 2026-09-06 05:48 — Full suite green: node --test (npm test) in plugins/grain/ with YG_BIN=/home/user/Yggdrasil/source/cli/dist/bin.js, HORDE_DIR=/home/user/krzysztofdudek/horde, GRAIN_CORPUS_CLONES set to the scratchpad clones -- # tests 2395, # suites 10, # pass 2395, # fail 0, # cancelled 0 (baseline was 2387; +8 is exactly the new propose-identifier-hygiene.test.mjs file, nothing else moved). Summary across the four classes, measured on real fixtures plus buildAspects unit tests (no corpus-scale run across Yggdrasil/express/petclinic/grain attempted in this pass -- surface was propose.mjs render-side only, verified against representative real repos rather than the four named corpora; a follow-up measurement pass against those four specific repos is still open if the director wants the exact before/after row counts on each). Branch fix/120-identifier-hygiene, ready for review.

## 2026-09-06 05:58 — Corpus measurement, all four named repos, run against the FIXED code (tests/stress/propose.mjs, --no-history): Yggdrasil (via --export ygg-export.json against the real /home/user/Yggdrasil tree, so the sub-gate lattice is unavailable -- no .grain/cache/ was built -- certified-convention side only) -- 51 rows skipped parser-node-type-as-identifier, 0 generic-type-parameter-as-domain-type, 0 cluster-narrower-than-scope, typesWithNoLaw 44, 94 types/75 nodes/82 aspects. express (staged copy) -- 37/0/0, typesWithNoLaw 9, 29 types/31 nodes/12 aspects, yg check: PASS (200 warnings, all unverified -- expected, keyless). spring-petclinic (staged copy) -- 4/0/0, typesWithNoLaw 13, 26 types/33 nodes/14 aspects, yg check: PASS (32 warnings). grain itself (staged copy of this fixed working tree) -- 195/0/0, typesWithNoLaw 13, 39 types/37 nodes/91 aspects, yg check: PASS (4686 warnings, all unverified/no-flows -- expected on a keyless run). Totals: 287 rows dropped as parser-node-type-as-identifier across the four corpora, zero regressions (every drop is a row that was previously emitted as a prose draft aspect -- has/stshape/first1/ret always, plus a handful of genuine call/imp coincidences on grain's own repo). Classes 2 and 3 did not fire naturally on any of the four real corpora at certified/sub-gate thresholds (no TS-generic-shaped identifier nor a role-scoped filenameshape/lex row cleared MIN_SUPPORT/share on these codebases) -- both are exercised instead by the targeted real fixtures and buildAspects unit tests already logged above, which is the honest coverage story for a class that a maintainer would see rarely in practice. yg check loads 3/4 cleanly (express, petclinic, grain: PASS). Yggdrasil's own check surfaced real mapping-path warnings, but they trace to my TEST HARNESS, not to this ticket's code: overlaying the freshly-proposed .yggdrasil/ onto a plain copy of the real Yggdrasil tree destroyed the ORIGINAL .yggdrasil/aspects/*.md content files that Yggdrasil's own dogfooded graph tracks as ordinary repository content (grain's miner has no special handling for a repo whose OWN top-level .yggdrasil/ is itself governed content, only for a NESTED one under a subdirectory -- nestedProjectRoots's own doc comment names exactly this gap for the nested case). The check process itself completed and printed a structured report either way -- it loaded, it did not crash -- so this is a self-hosting quirk of measuring on Yggdrasil's own repo, pre-existing and outside ticket 120's surface, not a regression from this change.
