
## 2026-09-02 05:05 — Director ruling esc-1 (2026-09-02): APPROVED — implement the per-literal check-side flag on top of research/042's already-shipped per-file quote convention. It is the per-literal APPLICATION of a convention already certified at file level, not a new acceptance gate; the delimiter-forced exemption is structural/derivable (a minority-quote literal containing the majority delimiter in its own body is exempt), not tuned. Measured on research/042: 22 genuine violations on express + 12 on flask, 0 false positives on telescope.nvim. Conditions from the ruling: (1) measure fire rate on clean, conforming files across >=3 repos and report it (037 standard: <5% good, double digits needs narrowing); (2) no new constant - the file-level convention's own acceptance decides whether the flag can exist; (3) the flag renders under the existing convention line, not as a new fact class; (4) tests required: the original 042 repro (7 single-quoted literals in a 100% double-quote file) flags; a delimiter-forced literal does not; a file with no certified quote convention flags nothing. See research/042's shipped withinFile disclosure (core.mjs) and docs/validation.md's quote-convention section for the exact mechanism to extend.
