
## 2026-09-02 06:46 — Instrument, not a fix: from the trial transcripts (.temp/stress/h040/ + prior trials), for each grain command: how often advertised (SKILL/hook text seen by the agent), how often invoked, and at what moment. Hypotheses to test: the SKILL text leads with where/check and buries the rest; obligation's trigger ('before creating a file') never fires because agents create files via Write without a pre-step; sub-agents (Explore) receive a different advertisement. Output: a reachability table per command + the concrete text/hook change that would surface obligation and completeness at the moment the catalog says the question is asked. Judge any change by re-running the trial harness. This gates every new command.

## 2026-09-02 07:16 — Measured reach over every transcript on disk: 23 pre-0.4.0 runs (46 transcripts) + the 13-run 0.4.0 trial = 63 agent-chosen CLI calls. THE LAW: 61 of 63 went to a command named in sessionContext()'s command block (where 31, check 29, status 1), 2 to a command named only in SKILL.md's frontmatter description (how, what), and 0 to any of the 11 named in neither. where opens the run (median position 0.009), check closes it (0.935), nothing between. H1 CONFIRMED and understated: 12 of 18 commands are absent from the advertisement entirely; obligation appears in exactly one markdown file in the whole plugin (commands/obligation.md), i.e. no surface an autonomous agent reads. H2 REFUTED TWICE: hooks.json ALREADY registers PreToolUse on Write -> check-hook --pre and it fired at all 8 file creations (proven by hook-seen.json/placement-pending.json/placement-outcomes.json written mid-run) - it computes placementHit only, and its call-to-action names 'grain where'; AND wiring obligation in would have changed nothing, since asked at all 8 real creation paths it answers 'nothing certifies' 8 of 8 (matching coverage 0.096 corpus-wide, and 0 of 86 birth events on this repo via selftest --obligation). H3 CONFIRMED on 0.4.0 data: 98 tool calls inside sub-agents, 0 grain calls; replay-4104e8c4's Explore made 43 locate calls in the same run whose parent called grain 8 times - root cause DIFFERS from 067's CLAUDE.md ruling, since grain advertises via SessionStart additionalContext which a sub-agent's own context never inherits. NEW FINDING: the push channel delivered 25 notes across the 7 with-arms (check 13, cochange 5, read 7) and the trial extractor counted 0, because stream-json carries hook records for SessionStart only; in replay-4104e8c4 the placement hook fired twice naming apps/e2e/tests/admin-panel/ — the truth commit's own directory — and recorded followed 0 / deviated 2, so the miss trial-0.4.0 calls 'unchanged' was grain being correct and overridden, not silent. RECOMMENDATION: (1) harness must read .grain/cache/{hook-seen,placement-outcomes,check-outcomes}.json, ~10 lines, doubles the observable surface; (2) gate a 17th command on 'does it certify often enough to deserve a slot', measured by its own selftest — obligation and completeness both fail that gate today; (3) the note that already fires at the creation moment was already right, so the supportable change is its wording, but n=2 is too small to ship on — which is why (1) comes first. Shipped: .system/research/command-reachability.md + one gate test asserting the roster is exactly [where, check, status, report] in order (red proven by temporarily adding a line to sessionContext(); engine reverted byte-identical). No engine/config/version changes. Suite 2129/2129, was 2128. Commit 521446f.
