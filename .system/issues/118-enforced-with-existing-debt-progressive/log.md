
## 2026-09-05 22:05 — start: read yg-config schema for progressive mode, set progressive reference in proposal, carry existingViolations in provenance + quiet report, verify on staged copy

## 2026-09-05 22:41 — done: propose derives the branch a change is measured against (origin/HEAD, else the current branch as the remote has it, else the local branch) and writes progressive.reference into the proposal's yg-config.yaml; left out with a one-line instruction in the report where the repository offers none. Every provenance.json and the --json report carry existingViolations; each enforced report line says 'N existing sites violate it today; progressive mode keeps them as warnings until touched' or, with no reference, that the first check is red on N sites. No status lowered. Verified on staged petclinic with one rule hand-promoted to enforced and the graph on the measured branch: clean tree PASS exit 0 with 5 pre-existing violations under 'enforced (outside changes)'; append one line to one of those files and it is FAIL exit 1 with 5 errors.
