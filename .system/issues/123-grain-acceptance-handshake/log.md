
## 2026-09-06 12:03 — Starting 123 on fix/123-adopt-handshake (merged claude/grain-agent-tool-b89y0x, includes 121's yg adopt + yg context --json, and 124's propose split into propose-*.mjs modules). Plan: item 1 handshake (propose next: line + YG_BIN dry-run summary + proposal.json provenance fields), item 2 oracle ratchet (file-size-budget enforced + prompt ceiling measured), item 3 125's classifier follow-up (exact tparams fact replacing looksLikeGenericTypeParam guess).

## 2026-09-06 12:58 — Item 2 (oracle ratchet) done: engine/file-size-budget advisory->enforced (all engine files under budget since 124, staged det check before/after byte-identical, zero new refusals). max_prompt_chars 600000->300000: measured the actual assembled reviewer prompt for every node carrying a prose aspect by reproducing Yggdrasil's own buildPairPrompt/assembledPromptChars (source/cli/src/llm/prompt.ts) against the staged oracle's real files (a real staged yg check --approve was tried first but aborts before any reviewer call on a pre-existing repo/docs deterministic refusal unrelated to this ticket) -- largest is plugin/engine/mining + engine/no-language-name-lists at 279770 chars, ahead of plugin/engine/queries (274928); 300000 is the smallest round ceiling above it. README.md dated note added, aspect-status counts and the file-size-budget/rule tables updated to match.

## 2026-09-06 13:09 — Item 1 (handshake) done: propose-report.mjs's next: line now names the transaction ('yg adopt <out> --dry-run' then 'yg adopt <out>'); cmdPropose (grain-export.mjs) resolves yg (YG_BIN/PATH) after writing the proposal and, when found, spawns 'yg adopt <out> --dry-run' and prints its summary verbatim under next: (a real run shows 'Already broken N sites' etc); when not found, prints what yg adopt would report and how to make it resolve. Verified proposal.json/provenance.json already carry everything adopt's readProvenance/readExistingViolations read (schema/engine/asOf/files, per-aspect existingViolations) -- confirmed empirically on a fresh real proposal, no fields were missing so no additive change was needed. Docs updated: docs/reference.md new handshake paragraph, commands/propose.md (description, relay-the-report bullet for the handshake, the do-not-run-yg-adopt-yourself line, and the now-stale generic-type-parameter guess description from 125 fixed too), README.md 'From clone to graph' closing sentence, plus two internal comments (grain-export.mjs's refusal message, propose.mjs's header) that still said 'moved in by hand'. Tests: propose-command.test.mjs gained 3 new tests under a new section 5 (next: line shape; with YG_BIN the real yg adopt --dry-run block appears verbatim below next: with Graph/Origin/Already broken/Blocks on and 'Nothing was written'; without YG_BIN the honest-negative message) -- had to narrow test 2's existing 'every line carries a number or path' quiet-report assertion to stop at next: since yg adopt's own verbatim prose (e.g. 'Nothing was written. Re-run without --dry-run to accept.') is not grain's wording to hold to that contract. All pass with and without YG_BIN set (66/66 across the five propose test files).

## 2026-09-06 13:09 — Item 3 (125 follow-up in the classifier) done: propose-aspects.mjs's looksLikeGenericTypeParam (CONVENTIONAL_TYPE_PARAM_RE guess + declaredTypeNames name-only census) replaced with isDeclaredTypeParam, an exact test against the row's own host site's tparams (own header) unioned with its OWNER type's tparams when the site is a method with .own set (export.mjs: site()/decorate() now also carry 'own' through to conformingSites/deviatingSites, additive, no version bump since 'own' was already cached, just not projected before; propose-lattice.mjs's sub-gate rows now carry the majority-side host site's own tparams/own too). typeTparamsByName (name -> tparams) replaces the old name-only declaredTypeNames census for the owner lookup. Fixture tests updated: propose-identifier-hygiene.test.mjs's old 'declared anywhere' escape-hatch test (which depended on the removed global census) replaced with three tests against the new per-row exact mechanism (own tparams drops it, shape alone never does, owner lookup via .own) -- the real class-2 fixture (identity<T> functions, already real generics) needed no change and still passes. Measured on a FRESH Yggdrasil export (scratch copy, git-history export, EXTR_V g34, tparams present -- 7/9216 conforming+deviating sites carry a nonzero tparams array): at the ROW level (34 certified conventions + 35 sub-gate rows carrying a ptype/returns/extends identifier) the old guess and new fact agree completely, 0 vs 0 excluded, zero disagreements -- none of the identifiers that actually survived to convention/sub-gate status on Yggdrasil happen to be type-parameter-shaped OR a declared type parameter today, so the fix changes no row's classification on this corpus right now (different from 125's own SCOPE-level count of 6+2 raw ptype/returns occurrences, which is a much larger, unfiltered population than what buildAspects ever sees as a row). Tests: propose-identifier-hygiene.test.mjs 10/10 pass; type-parameter-fact.test.mjs 11/11 pass (untouched); broader propose suite (propose.test.mjs, propose-hardening, propose-hostile-repo, propose-domain-cut-predicate, propose-label-partition-host, propose-language-marker-files, propose-own-state-never-proposed, propose-type-levels, propose-zero-relations-wording, cross-check-propose-parity, propose-absence-not-forbiddance) 92/92 (2 skipped); cross-check-export-parity + canonical-exemplar 20/20 (export.mjs field additions didn't disturb any pinned cross-check).

## 2026-09-06 13:37 — Fixed a genuine pre-existing bug found while testing item 3: tests/grain.test.mjs and tests/answer-grammar.test.mjs both spawn 'grain export' via spawnSync with no explicit maxBuffer, defaulting to node's 1MB ceiling; export.mjs's additive 'own' field (item 3) tipped three of their fixtures' export JSON just over it, which fails SILENTLY (status: null, or truncated stdout -> JSON.parse throws) rather than as a reported overflow -- a latent bug any future additive export field could trip, not specific to this change. Fixed by adding maxBuffer: 1<<28 to both files' grain() helpers, matching the bound already used for the same reason elsewhere in the suite (e.g. propose-command.test.mjs). Own commit. Full npm test with YG_BIN/HORDE_DIR/GRAIN_CORPUS_CLONES: 2415/2415 pass, 0 fail.

## 2026-09-06 13:38 — All three items committed on fix/123-adopt-handshake (4 commits: 89cb56b handshake, 1e815b4 oracle ratchet, 0667713 maxBuffer bugfix, 6374888 classifier exact fact). Full npm test (YG_BIN, HORDE_DIR, GRAIN_CORPUS_CLONES): 2415/2415 pass, 0 fail. Ready for review.
