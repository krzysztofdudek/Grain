
## 2026-09-01 23:45 — Spec = question-catalog.md §6a and §4.1. Three mechanical adoption bugs, no engine work: (1) ship a bare 'grain' shim on PATH or advertise a form not starting with a runtime's name — one agent generalised a pnpm denial into 'node blocked' and never tried grain; (2) put the advertisement where the searching happens — 4 of 6 replay runs delegated all locate work to an Explore sub-agent whose prompt never mentions grain (83 blind locate calls); (3) say file or directory unambiguously — an agent had the correct answer ModeToggle.tsx on screen and treated it as a directory. Adoption is inversely correlated with need: the 5 runs that ignored grain were the 5 most expensive.

## 2026-09-02 00:23 — shipped (a) session-context wording no longer opens with 'node' (grain.mjs) and (c) where/check's 'in:' locator now carries a trailing / so a directory context line can't be misread as the file hit below it (core.mjs); (b) is a bounded limitation — confirmed via Claude Code docs that the built-in Explore agent (the actual delegate in 4/6 replay runs) explicitly skips CLAUDE.md loading, so a repo-root CLAUDE.md advertisement would not reach it (other subagent types would see it, but Explore is the one that mattered here); 2 tests added, full suite 1960/1960 (4 pre-existing unrelated TODOs), no engine/model/config changes
