
## 2026-09-02 02:57 — Root cause: not a grammar limit but grain's own walk stopping descent at every ERROR node (extractScopes, core.mjs) — tree-sitter's error recovery already parses clean children (object/class/def) beside the unparseable @Inject() constructor; fixed by pushing an ERROR node's own children onto the walk, extracting nothing from the ERROR node itself. Overlaps §053's root cause exactly (same 97/843 hasError files, same idiom) but is a distinct, previously-unfixed defect. Measured on playframework@61ec059: 58/97 error-bearing files had >=1 salvageable declaration, 145 recovered total (81 types, 58 methods, 6 finally). hasError unchanged, so 053's parse-degraded caveat still fires for all 97 files. New test tests/scala-error-region-salvage.test.mjs (13 tests, red without fix, green with). Full suite green (2011/2011). Scala extraction output changes — flagged for next batched EXTR_V bump, config.mjs untouched.
