
## 2026-09-06 04:52 — Horde branch claude/grain-agent-tool-b89y0x started: baseline npm test in skills/horde/scripts green (297 tests, exit 0). Working items 1-7 in order, one commit each.

## 2026-09-06 05:02 — Item 1 done (Horde c3e01df): premerge gains a 'graph' checklist item — with nodeSource yggdrasil it runs 'yg check' in the branch's own worktree on every run, whatever config.gates holds; red graph = red gate, unstartable CLI = red with config.ygCommand named, manual mode has no such item. horde init on a graph repo says it put it there. New config key ygCommand routes every yg call. 5 new tests.

## 2026-09-06 05:09 — Item 2 done (Horde 81e1698): node.mjs show now opens with a Rules block — every aspect in force on the node with its status word (enforced blocks / advisory warns / draft inert), from 'yg context --node' (JSON form when the CLI has one, text otherwise) or, with no CLI, from the graph files (own + cascaded node aspects, own + ancestor type aspects, effective status = max), which names itself as such. Charter's 'Rules inherited from above' reproduced under it. yg-node.yaml parser now keeps the aspects section it used to discard. Verified both paths agree on petclinic (13 rules) and on the express oracle (11 shared; yg additionally resolves the port channel). 8 new tests.

## 2026-09-06 05:25 — Items 3+4 done (Horde 3e79ea7, 19a186c): premerge's revert test now has no built-in test-glob default — unset config.testGlobs is a REFUSAL naming the setting, a pass names the patterns it looked for. horde init reads the build files (npm/Maven/Gradle/Cargo/Go/Python/Make) for both the gate command and the test patterns and reports what it worked out or that it worked out nothing and asks; --test-globs names them outright. config set now writes a real list for list-valued keys (comma form or JSON array), refusing unreadable text. 13 new tests. Note: a pre-existing roster liveness test (1-second window) flakes on this machine at origin/main too — not caused here.

## 2026-09-06 05:34 — Items 5+6 done (Horde d296c80, 47e9e93): 'horde.mjs charter show|edit' writes the mission charter from stdin like node.mjs charter edit, reporting what the rewrite did to the evidence catalogue and warning when it drops a row a verifier already proved; 'queue.mjs set <t> merged --sha' now appends the wave-journal bullet itself (idempotent, wave.mjs merged still works and never double-records), so the evidence catalogue goes green on one write — proven end to end by a test that merges and closes the wave.

## 2026-09-06 05:46 — Item 7 done (Horde c226f73) and branch pushed: 7 commits on claude/grain-agent-tool-b89y0x, full suite 328/328 green. tk new refuses 3+ nodes; verify.mjs takes --revert no-new-tests so a refactor/rename/config ticket can be verified at all; README says what to do about the files a skill install adds to a graphed repo's coverage; a pre-existing liveness test that judged a steward by machine speed made deterministic. Re-ran the dry run on a fresh petclinic propose (33 nodes): premerge on the enforced-rule break now prints '✓ gate — green (./mvnw -B test)' next to '✗ graph — yg check exited 1 — the graph refuses this tree ... a red graph is a red gate', exit 1; node.mjs show src/main/java/org lists all 13 inherited rules with statuses (12 draft, 1 advisory) from yg context --json, and the same 13 with the same statuses from the graph files with no CLI present.
