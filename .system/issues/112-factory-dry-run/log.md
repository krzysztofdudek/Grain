
## 2026-09-05 19:57 — FACTORY DRY RUN start: branch research/112-factory cut from claude/grain-agent-tool-b89y0x (worktree HEAD was stale at 9868c31). Read 112 issue, ecosystem-design, counsel-factory, integration-stress corpus matrix, ticket 100 log, and the whole Horde skill (SKILL.md, model.md, topology.md, all role briefs, node.mjs, horde.mjs). Target: spring-petclinic (integration-stress row: 132 files, 12 types / 14 nodes / 59 aspects 38det+21prose, 87% det sense rate) — Java support is adequate, no fallback needed. Stage: scratchpad/w112/.

## 2026-09-05 20:04 — STEP 1+2 done. propose (32s wall, YG_BIN set): 12 types / 14 nodes / 0 relations / 0 cycles; 18 aspects = 0 enforced + 9 advisory + 9 draft (6 prose-unenforceable-keyless, 3 no-catch); 12 rendered checks, 82 drill cases, no hold-out; 270 lattice rows, 62 in the sub-gate band; 14 charters avg 54.4 lines. Zero enforced because every drill-clearing rule came from the sub-gate lattice, never from a certified convention. Adoption: mv proposal to .yggdrasil/ -> yg check PASS (227 warnings, 225 unverified pairs); yg check --approve --only-deterministic -> 181 deterministic verdicts, 44 refused, and all 44 are advisory warnings on untouched HEAD (a standing noise floor), still PASS; yg init --upgrade for the digest. FOUR GRAIN BUGS found, all wrong output: (1) all 14 charter.md say "none certified" and "none below the bound" because the charter renderer matches aspect.host (a TYPE id, src-main-java) against node.id (a PATH id, src/main/java) - the one artifact Horde reads carries zero rule information; (2) a promoted check.mjs still carries the DRAFT header saying the runner never executes it; (3) deco statements render @@SpringBootTest; (4) nameshape renders "the name shape ``" and filenameshape falls through to "have auto.filenameshape".

## 2026-09-05 20:35 — STEP 3-5 done, all played by hand (no Agent Teams, no keys needed anywhere: every yg leg was deterministic and free). Horde installed by copying skills/horde into .claude/skills/ - that alone adds 52 tracked files and drops Yggdrasil coverage from 96% to 70% (uncovered 56). WHAT HORDE READS OF GRAIN'S THREE OUTPUTS: yg-node.yaml (name/type/description/mapping/relations - its parser recognises an "aspects" section header and then discards it, and the proposal does not put aspects there anyway), charter.md verbatim, contracts.md; NOT aspects, not statuses. Measured side by side on the same file: yg context --file Visit.java prints all 8 rules with [advisory]/[draft] in 31 lines; node.mjs show src/main/java/org prints 72 lines with zero rules; the 282-line worker brief embeds three charters, all three saying "none certified". The only rule that reached the worker was the sentence I typed into the ticket by hand after reading yg context. THE CHANGE: Visit.notes + @Size(255), three schema.sql, ownerDetails.html, 11 message bundles, VisitValidationTests (2 tests). Advisory refusals 44 before and 44 after - not one aspect fired on the change; 18 of 18 rules irrelevant, and the 2 that produce a verdict on Visit.java say "this file should not import jakarta.persistence.Entity/Table", which is wrong law on the exact file the requirement touches. What DID catch a real defect was the verifier's gate: adding a column broke db/h2/data.sql positional INSERTs, 16 errors, verdict not-reproduced, worker fixed, second gate 76/0/0 green. Nothing in the graph could have known - 0 relations were mined. BREAKS: promoting partition-nameshape to enforced by hand and adding a lowercase class -> yg check FAIL exit 1, Errors (1) enforced 1 pair; same file with the rule back to draft -> PASS (47 warnings) exit 0, warn not block. But premerge on that same broken branch printed "gate green (./mvnw -B test)" - Horde's gate never runs yg check. Also: premerge's revert test said "no new test files in diff" for the ticket whose evidence IS a new test (default globs are **/*.test.*), horde.mjs config set cannot store an array so the fix crashes premerge until config.json is hand-edited, tk new accepted a three-node ticket, evidence only goes green if the merge is recorded twice (queue.mjs set merged AND wave.mjs merged), and no tool writes the mission charter at all.
