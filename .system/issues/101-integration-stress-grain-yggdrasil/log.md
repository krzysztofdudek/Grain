
## 2026-09-05 07:02 — SPEC (director z rulingu granularity-bounded-by-evidence-not-taste). Cel użytkownika: 'zrób stress testy takiej integracji i zobacz, na ile Grain sensownie gada vs na czym operuje Yggdrasil'. Instrument plugins/grain/tests/stress/integration-stress.mjs nad corpus.json (najpierw repo z zimnym buildem < 3 min: petclinic, CleanArchitecture, chi, gin, axum, express, flask, sinatra, Slim, telescope, zig-zls, bash-it, openzeppelin…; Symfony/curl/nest osobno lub pominięte z powodem) + hostile repos z edge-cases.mjs. Per repo: (1) grain export → propose (w trybie NAJDROBNIEJSZYM z dowodem) → staged yg init --no-reviewer + yg check: ładuje? błędy per kod; (2) yg check --approve --only-deterministic na kopii: ile par, ile werdyktów, ile odmów na własnym kodzie repo (= reguła coś łapie już dziś) — z podziałem draft/advisory; (3) yg drill na każdym check.mjs: pass/MISS/FA — FA musi być 0; (4) SENSE RATE = elementy spełniające (ładuje ∧ ma pary ∧ drill łapie ∧ FA=0) ÷ wszystkie wyrenderowane; osobno dla typów, węzłów, aspektów; (5) rozkład granularności: pliki per typ/węzeł, reguły per partycja, ile jednoplikowych; (6) hostile: propozycja na 25 repo z edge-cases degraduje bez crasha i bez fabrykacji (pusty graf jest OK, zły graf nie); (7) czas i pamięć per repo. Wyjście: macierz repo × (ładuje, pary, łapania, FA, sense rate, granularność) + raport .system/research/integration-stress.md. Zero zmian silnika; podłogi instrumentu nazwane i zmierzone (MIN_TYPE_FILES z 094 — zmierzyć jego usunięcie). Yggdrasil read-only; własne klony w scratchpadzie. Opus (pomiar + osąd, jak 093/096). Po 097/098/100.

## 2026-09-05 09:15 — MEASURED (worker, Opus). Instrument plugins/grain/tests/stress/integration-stress.mjs + guard plugins/grain/tests/integration-stress.test.mjs (15 tests, red-green on every fix). Metric per ruling granularity-bounded-by-evidence-not-taste: SENSE RATE = rendered elements that (load AND produce pairs with a verdict AND have a drill that catches >=1 violates AND 0 FALSE-ALARM) / rendered, per kind, as a nested funnel. Two structural facts the measurement had to be built around and that the report states: (1) a proposal AS SHIPPED produces ZERO pairs — every aspect is status: draft and draft removes the pair from the expected set, so the sense rate is only measurable on a stage where the deterministic aspects are promoted to advisory; (2) promoting a PROSE aspect with no reviewer: in yg-config aborts yg check --approve entirely ('A judgment rule has no judge'), so prose aspects have sense rate 0 under a keyless gate, structurally — counted, not hidden. FIVE defects found by running through Yggdrasil's real binary, all fixed on sight with red-green tests, engine untouched: (a) module's allowed parents excluded the proposal's own types -> parent-type-forbidden, a BLOCKING error; (b) the imp check matched a specifier only inside quotes, so it was inert in Java/Python/Rust/C# — 17 of 38 checks on spring-petclinic scored 0 refusals and 4-5/5 MISS; (c) filenameshape tested an anchored shape against the name WITH its extension — both such checks refused 100% of their own scope and one FALSE-ALARMED on 5/5 of grain's own conforming sites; (d) the family adapter's fitted predicate selected NONE of its members even on family-planted-mono (the fixture 100 called exact) and folded the same-language decoy into the polyglot TS family; (e) propose.mjs crashed (exit 128, 'not a git repository') on a directory of code with no git. Worth of (b)+(c) on spring-petclinic, same flags: deterministic-aspect sense rate 24%->87%, FALSE-ALARM 5->0, refusals on own code 100->176 of 915. Yggdrasil's own seam numbers moved too: yg drill 37 pass/16 MISS -> 42/11, families nominated 5 -> 7.

## 2026-09-05 09:20 — CORRECTION to the previous entry: the imp-check count on spring-petclinic is 24 of 38 rendered checks, not 17 (recounted from the pre-fix per-aspect table: 5 in src-main-java, 19 in src-test); every one of the 24 scored 0 refusals and 4-5 of its 5 violates- cases MISS. Everything else in that entry stands.

## 2026-09-05 10:42 — FINAL (worker). CORPUS: 17 measured of 18 attempted (cpp-json timed out at 1500s in grain export's history walk, 999 MB peak — reported, not dropped), 9 languages, 6635 tracked files. 17/17 LOAD with ZERO load-blocking error codes. 394 types / 414 nodes / 1671 aspects (366 deterministic + 1305 prose) -> 17704 pairs, 2164 refusals on the repos' own code, drill 1345 pass / 463 MISS / 6 FALSE-ALARM. POOLED SENSE RATE: types 12% (394->394->64->50->46), nodes 36% (414->414->189->157->151), aspects-all 13%, DETERMINISTIC aspects 57% (366->366->348->214->210), prose 0%. What dominates: 1305 of 1671 drafted aspects are prose and cannot be operated on keylessly at all (that one fact takes 57%->13%); among scripts 348/366 produce pairs and the loss after that is the drill; only 70 of 394 types carry ANY aspect; 125 of 366 rendered checks refuse nothing anywhere in their own repo today; FA=0 is a real claim for only 88 of 366 (the rest have no satisfies- case). GRANULARITY: 0 of 414 nodes map exactly one file (27 map none); median aspects-per-type is 0 in EVERY repo while the max runs 7..86 — a handful of partitions carry the whole rule set; ownership sizing median ~4k est. tokens, p90 ~58k, 13 of 394 (3%) over the 200k budget. HOSTILE: 17 of 17 repositories from tests/stress/edge-cases.mjs hold the contract (no crash, no graph yg refuses, no claim over zero evidence) after fixing the no-git crash; empty/nocode/tests-only produce an empty-or-minimal proposal that still loads. FLOOR: MIN_TYPE_FILES 2 vs 1 on 3 repos — removing it costs 4 types and 3 nodes and changes NOTHING else (aspects, pairs, refusals, drills, FA byte-identical); type/node sense rates go DOWN 10->9% and 29->28% because every element it suppressed is a rule-less directory. The floor is not load-bearing. SIXTH DEFECT found from the matrix: grain's synthetic partition name _root rendered as a directory path — 4 of 17 repos, 168 aspects scoped to _root/** (17 deterministic), ALL producing zero pairs, plus mapping-path-missing/type-when-mismatch; a git submodule gitlink rendered as a file did the same. Fixed (derived test: a partition name under which no tracked file lives is not a directory) and re-measured: gin 148->62 aspects with pairs/refusals unchanged and det-aspect sense 55%->86%; leveldb and kotlin-datetime go from 37/46 aspects producing nothing to ZERO aspects and (leveldb) zero graph errors; spring-petclinic control byte-identical. Corpus-wide arithmetic over verified-zero contributions: det-aspect rate 57%->60%, nodes 36%->37%. Full suite 2260/2260 green (2244 + 16 new).

## 2026-09-05 10:42 — JUDGING (bar 2 of 097, independent Opus judge — NOT the author of law-loop.mjs; disclosed in the report as a model classifier per ruling layers-compatible-no-user-thresholds). 20-row sample of law-loop-yggdrasil.md §7 classified (a)/(b)/(c)/(d) with one line of evidence per row read from Yggdrasil's actual source, not from the table. Result: (a) miner miss 0 · (b) graph debt 6 (rows 1,12,13,14,15,16 — single quotes x4, 2-space indent, PascalCase type names: real undeclared practice, no eslint rule for any of them, none of the 51 hand aspects covers them) · (c) undecidable 0 · (d) not a rule at all 14. PRECISION on (a)+(b): 1/11 = 0.091 on the ELEVEN rows that have a held-out corpus (the population the bar is about), 6/20 = 0.300 over all twenty (a mixed population). BAR WAS >=0.80 — FAILED on both, not a near miss. Sensitivity stated: if house style is held to belong to a formatter rather than the graph, five of the six (b) rows become (d) and it is 0/11 and 1/20=0.05; the verdict does not change. The 14 (d) rows fall into four named modes: (1) minority usage read as a negative rule — 8 rows, e.g. node:url in 82/346 unit files, src/model/graph in 87/346, relations/pass in 15/68; (2) an identifier that is not an import — rows 9 and 11 name 'l' and 'line', and NO import specifier in tests/e2e has either as its last path segment (they are loop variables); (3) a counting-unit artefact — row 19 says 'do not call it' at 0.796 while 67 of 68 integration files call it(, because grain counts SCOPES and the callbacks inside it() do not themselves call it; (4) an INVERTED direction against an existing hand rule — src/utils/posix.ts documents itself as 'the single home for the normalize path separators idiom' and two hand aspects (posix-paths-source, posix-paths-output) push code toward it, while row 20 says do not import it; e2e-public-surface explicitly permits shared e2e helpers while row 6 says do not call one; test-deterministic requires fresh temp dirs while row 18 says do not call rmSync. Mode 4 is the sharpest confirmation of ruling law-loop-b1-not-doing-it-with-numbers: grain sees exactly the identifier the maintainer's rule is about and renders the rule with the opposite sign. Not one row was (a) and not one was (c) — every row was decidable from the code, so the sample is not hard, it is mostly artefactual.

## 2026-09-05 10:43 — FAMILY ADAPTER (100's disclosed precision gap), measured and fixed. The 100 report's characterisation was half wrong and the wrong half was the worse one: there is NO cross-language merge — the language stratum holds, py family is exactly the 5 planted *_repository.py and the ts family is ts-only. What actually happened on family-planted-polyglot: 2 families, 11 members, 1 wrong (src/ts/ConfigLoader.ts, the SAME-language decoy the fixture's README says must never cluster) = member precision 0.909; and the ts family's fitted predicate \b[A-Za-z0-9_]*first[A-Za-z0-9_]*\b matched 0 OF ITS 6 MEMBERS. On family-planted-mono — the fixture 100 called EXACT — the same predicate matched 0 of 5: the membership was right and the rule attached to it was vacuous, which is worse than a visibly wrong member because nothing in the output's shape says so. Two instrument-level fixes, both of which can only remove: (1) a case-folded nameTokens entry is rendered case-tolerantly ([Ff][Ii][Rr][Ss][Tt]) — it comes from core.mjs's tokenize, which lowercases, so rendering it literally is vacuous in camelCase and only this branch of contentRegexFor needed it; (2) a predicate-fit gate — a member the family's own fitted predicate does not select is dropped, and a family left under FAMILY_MIN_MEMBERS is dropped whole. After: polyglot 2 families / 10 members / 0 wrong / predicate selects 10 of 10; mono still exactly 1 family of 5, now with a predicate that selects all 5. Guarded red-green against a synthetic fixture reproducing the polyglot shape (both languages, both decoys). Seam test unchanged-green and its Yggdrasil numbers moved in the right direction: yg drill 37 pass/16 MISS -> 42/11, families written+nominated 5 -> 7. NOT FIXED, deliberately (escape hatch of fix-bugs-on-sight — it is an acceptance decision about the instrument's own ground truth, and it would move the FALSE-ALARM figure in three published reports): a file-scoped check rendered from a SUB-FILE-scoped convention can false-alarm on a file the corpus labels conforming. All 6 FALSE-ALARMs in the entire ticket are this and only this, in 4 aspects, 3 of them the returns template — e.g. axum/src/boxed.rs:56 has 'fn fmt(...) -> fmt::Result' inside a file cut as a satisfies- case for 'methods here do not declare a return type of Result'. cutDrills already has the rule that fixes it ('a file carrying ANY deviating site is a violates- case') but consults only the export's deviating-site sample; the completion is to verify the satisfies- label against the file's bytes. Filed with evidence in the report.

## 2026-09-05 10:50 — HUB VERIFICATION (director): diff 6 files, engine untouched, revert-test red on base, suite 2260/2260, Yggdrasil/Horde clean. Accepted as measured. Headline: loading is solved (17/17, zero load-blocking codes); the binding constraint is that 1305/1671 drafted aspects are prose (0% operable keyless) and only 70/394 types carry any aspect; where a rule is a script it works (348/366 pairs), then the drill costs (125 catch nothing, 6 FA all file-scoped checks from sub-file conventions). Bar 2 of 097 FAILED at 0.091/0.300 (14 of 20 rows judged 'not a rule') — confirms law-loop-b1 ruling from the other side. Six renderer defects found only by the neighbour's real binary, fixed red-green (petclinic 24%→87%, gin 55%→86%). MIN_TYPE_FILES not load-bearing (4 types + 3 nodes on 3 repos, nothing else moves).
