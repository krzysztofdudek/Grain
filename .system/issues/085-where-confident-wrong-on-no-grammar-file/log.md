
## 2026-09-02 08:00 — instrument A's checkNoDeclarationsAnywhere sample (12 identifiers per repo that occur ONLY in no-grammar files, never in code) measured a confident-but-wrong where top hit on 20 of 22 corpus repos this wave, at 1/12 (8%) to 7/12 (58%) of the sample (bash-it worst: 7/12, all .editorconfig keys — 'EditorConfig', 'indent_style', 'indent_size', 'end_of_line' each return a score>=0.3 top hit elsewhere in the repo that never mentions the term at all). This is a confident, specific, WRONG claim (not silence) — the failure class instrument A exists to catch — but is narrow in what it affects: identifiers whose only real occurrence is a file grain assigns no grammar to (config/dotfiles, docs), and it is plausibly an inherent property of lexical/content ranking scoring common short config words against unrelated code by coincidental token overlap, not an isolated bug in one code path. NOT yet diagnosed to a specific root cause or fix — this ticket records the measurement so a future wave can decide whether it is fixable (e.g. suppressing a low-margin top hit when the query's dominant token only appears in a no-grammar file grain can see but not index) or is a boundary to disclose (per the where 'zero lexical foothold' precedent, ticket 070, already fixed for a related-but-distinct denominator). Repro: node tests/stress/audit-claims.mjs <any-corpus-repo> --json-out out.json; byType.noDeclarationsAnywhere.samples. Per-repo rate this wave (fabricated/checked): CleanArchitecture 3/12, Slim 1/12, bash-it 7/12, cpp-json 1/12, express 1/12, flask 3/12, gin 5/12, groovy-spock 5/12, kotlin-datetime 5/12, leveldb 1/12, nest 1/12, okhttp 2/12, openzeppelin-contracts 2/12, playframework 6/12, serde-full 1/12, sinatra 2/12, symfony-shallow 3/12, telescope.nvim 1/12, tsx-zustand 2/12 (axum-full, kotlin-datetime already counted, spring-petclinic, curl, zig-zls, Slim, bash-it also 0-checked or already listed; 0/12 or untested on the remainder).

## 2026-09-02 09:03 — MEASURED (research/085, Opus): the 8-58% is instrument A's first-12 sample of identifiers occurring only in no-grammar files; it reads --json hits only, so it cannot tell 'confidently wrong' from 'wrong but already disclosed'. Reproduced in-process on 4 real repos (spec-kit 1042 cands, supersplat 67, opencode 400, openclaw 400). 92.5%/97%/79%/68% of candidates take 057's zero-hit path and are disclosed correctly; the failures are ALL on a THIRD path -- nonzero hits, so cmdWhere never calls findUngrammaredHit (gated on !hits.length). Splitting the silent-confident-wrong rows: those whose query is an identifier-shaped COMPOUND (whereCmd's own qraw: tokenize>=2 or ._$) are the real 085 shape (11/21/29); the rest are single common English words (40/26/49) where grain answering with code is correct and instrument A's oracle is wrong. Verdict: WIRING GAP, not a structural boundary.

## 2026-09-02 09:20 — FIXED (research/085): extended 057's honest-negative path to its THIRD entry point. whereCmd now returns unknownIdent (query is ONE word, is identifier-shaped per its own qraw, and no card's names declares it -- all three already computed, no new constant); cmdWhere pays for the same bounded findUngrammaredHit scan on that cue, and whereCmd discloses ahead of 070's banner when the scan hits. Recall on the real shape 61/61 (spec-kit 11/11, opencode 21/21, openclaw 29/29); 0 fires on a hit that points at truth. Fire rate on ordinary queries 4 repos: spec-kit 0/220, supersplat 0/544, opencode 1/538, openclaw 0/548 = 1/1850 (0.054%) vs 070's 0.048% and the 037 1.58% bar. 057's scan discipline kept: the one-word clause takes new scans on commit-message queries from 36.9% to 0%. Suite 2135 pass / 0 fail (2129 base + 6 new, red-green proven).

## 2026-09-02 09:48 — ORDERING CORRECTION (research/085): the 085 banner must run AFTER where's existing disclosure ladder, not ahead of it. Placed first it pre-empted the ladder's last arm, which SUPPRESSES an uncorroborated top hit outright (hits=[], 'no confident match') -- measurably reviving 6 of opencode's 84 ranked rows the suppression arm had been discarding. Moved after the ladder and gated on hits.length: a suppressed answer stays suppressed and falls through to 057's own message, which names the same never-parsed file anyway. ranked-path counts now identical to pre-change (spec-kit 78, opencode 84). Pinned by a regression test. Suite 2136 pass / 0 fail.
