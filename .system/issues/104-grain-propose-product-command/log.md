
## 2026-09-05 12:29 — Orzeczenie propose-default-is-quiet. Zakres: (1) komenda plugins/grain/commands (wzorzec istniejących komend, hooks.json, skills/grain/SKILL.md), która woła logikę propose.mjs przeniesioną do engine/ lub commands/ bez zmian semantyki (instrument tests/stress/propose.mjs zostaje jako cienki wrapper albo znika, jeśli testy przejdą na komendę); (2) domyślne wyjście ciche wg orzeczenia, --full pokazuje wszystko; (3) instrument reachability (081) na nowej komendzie; (4) jeden ustalony export Yggdrasil → policzyć jeszcze raz enforced/no-catch/FA i wyjaśnić rozjazd 11/13 vs 10/33; (5) docs: commands/*.md, docs/reference.md 'The proposal contract' (statusy bez zmian), README krótka sekcja 'od klona do grafu'; (6) e2e test przez publiczne CLI na realnym fixture + yg check na wyniku. Bez nowych stałych.

## 2026-09-05 12:31 — Start: branch fix/104-propose-command off a8bfde3. Read ticket, ruling propose-default-is-quiet, four 101 rulings, reachability law.

## 2026-09-05 12:43 — Renderer moved to plugins/grain/engine/propose.mjs (verbatim); the Yggdrasil-graph YAML/glob/readGraph block moved out of tests/stress/reconstruct.mjs into plugins/grain/engine/yggdrasil-graph.mjs (verbatim, re-exported from reconstruct.mjs so every consumer keeps its imports) so a product command no longer depends on a test instrument. tests/stress/propose.mjs is now a thin wrapper: it re-exports every name it exported before, keeps scoreProposal and the whole CLI, and injects its own DEFAULT_YG_BIN so measurement runs are unchanged; the engine resolves YG_BIN or a yg on PATH and hardcodes no machine path. Byte-identity: rendered /home/user/Yggdrasil from the fixed ygg-export.json with HEAD code and with the moved code into the SAME out-dir path — diff -r reports zero differences, so not even a path or timestamp moved. Numbers reproduced: 82 types, 73 nodes, 124 aspects (33 check / 91 prose), 10 enforced, drafts {prose 91, no-catch 22, fa 1}, 665 drill cases.
