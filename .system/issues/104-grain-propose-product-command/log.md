
## 2026-09-05 12:29 — Orzeczenie propose-default-is-quiet. Zakres: (1) komenda plugins/grain/commands (wzorzec istniejących komend, hooks.json, skills/grain/SKILL.md), która woła logikę propose.mjs przeniesioną do engine/ lub commands/ bez zmian semantyki (instrument tests/stress/propose.mjs zostaje jako cienki wrapper albo znika, jeśli testy przejdą na komendę); (2) domyślne wyjście ciche wg orzeczenia, --full pokazuje wszystko; (3) instrument reachability (081) na nowej komendzie; (4) jeden ustalony export Yggdrasil → policzyć jeszcze raz enforced/no-catch/FA i wyjaśnić rozjazd 11/13 vs 10/33; (5) docs: commands/*.md, docs/reference.md 'The proposal contract' (statusy bez zmian), README krótka sekcja 'od klona do grafu'; (6) e2e test przez publiczne CLI na realnym fixture + yg check na wyniku. Bez nowych stałych.

## 2026-09-05 12:31 — Start: branch fix/104-propose-command off a8bfde3. Read ticket, ruling propose-default-is-quiet, four 101 rulings, reachability law.

## 2026-09-05 12:43 — Renderer moved to plugins/grain/engine/propose.mjs (verbatim); the Yggdrasil-graph YAML/glob/readGraph block moved out of tests/stress/reconstruct.mjs into plugins/grain/engine/yggdrasil-graph.mjs (verbatim, re-exported from reconstruct.mjs so every consumer keeps its imports) so a product command no longer depends on a test instrument. tests/stress/propose.mjs is now a thin wrapper: it re-exports every name it exported before, keeps scoreProposal and the whole CLI, and injects its own DEFAULT_YG_BIN so measurement runs are unchanged; the engine resolves YG_BIN or a yg on PATH and hardcodes no machine path. Byte-identity: rendered /home/user/Yggdrasil from the fixed ygg-export.json with HEAD code and with the moved code into the SAME out-dir path — diff -r reports zero differences, so not even a path or timestamp moved. Numbers reproduced: 82 types, 73 nodes, 124 aspects (33 check / 91 prose), 10 enforced, drafts {prose 91, no-catch 22, fa 1}, 665 drill cases.

## 2026-09-05 12:51 — grain propose landed as a dispatcher command: cmdPropose in engine/grain.mjs, out-dir default <root>/.yggdrasil-proposal/ (refuses the repo root and the repo's own .yggdrasil/), self-ignoring .gitignore written into the out-dir on first run so a staging tree can never be committed. Report built once by proposeReport() in engine/propose.mjs for both text and --json, so the two cannot disagree. Quiet default = architecture counts + aspects that earned enforced (with each drill's caught/planted/false-alarm numbers) + candidates; a CANDIDATE IS DEFINED, not thresholded: a draft the same real drill caught >=1 violation with, which is the bar no-catch-rules-stay-draft already sets — so no display cap and no new constant, and with no drill there are simply no candidates and the report says so. Reachability: ONE unindented conditional SessionStart line, gated on (index exists AND no .yggdrasil/); SKILL.md description + Developer commands, USAGE, commands/propose.md, docs/reference.md command row + rewritten proposal contract, README 'From clone to graph'.

## 2026-09-05 13:00 — Ticket 102's 13-deterministic/11-enforced numbers REPRODUCED exactly, and the discrepancy explained: it is --no-history. Ran the moved renderer on a local clone of Yggdrasil at the same HEAD 5cca6b15 with --no-history: grain export reports '0 conventions' (without the history layer nothing counts as established), so the certified-convention side of buildAspects produces nothing at all and every draft comes from the sub-gate lattice, which is available because a working copy the export just built carries a .grain/cache. Result: 82 types, 72 nodes, 284 alternatives, 102 aspects (13 check / 89 prose), 353 drill cases, latticeRows 6552, subGate 587, 11 enforced, 2 no-catch, skippedNotARule 0 — field-for-field identical to 102's own proposal.json, including its proseByClass (dir3/first1/namesuffix are lattice pids, not convention enumerators). The director's wave-8 render used the FULL-history fixed export against /home/user/Yggdrasil, which has no .grain/cache: latticeRows 0, so all 124 drafts come from the 182 certified conventions (33 renderable as checks, 37 filebirth skipped as not-a-rule, 17 group-scoped unrenderable), 10 enforced, 22 no-catch, 1 fa. Same code, same commit, two export shapes: one measures the certified side, the other measures the sub-gate side. Neither number is wrong and neither is comparable to the other. The product command always exports WITH history unless the caller passes --no-history.

## 2026-09-05 13:05 — Default report on Yggdrasil's own auto-proposal (rendered from the fixed export, real drill against the built yg): 24 lines for a 124-aspect proposal — one architecture line (82 node types, 73 nodes, 210 relations, 8 cycles), 10 enforced rules each with 'caught N of M planted violations, 0 false alarms' beside its practised share and n, exactly ONE candidate (partition-call-mkdirsync: caught 5 of 5 but 4 false alarms, held as file-scope-approximation-fa), and one counted line for the 113 drafts + 284 alternatives + 37 not-a-rule skips that stayed on disk. Bug found and fixed on sight while the suite ran: the instrument wrapper used 'export {…} from' for the names it re-exports, which does NOT bind them in the module's own scope — buildFamilyCandidates was undefined in its CLI, breaking integration-stress.test.mjs's family-adapter test. Same trap avoided in reconstruct.mjs; both now import-then-export. Every wrapper branch (--score, --family-candidates, --json) exercised end to end afterwards.
