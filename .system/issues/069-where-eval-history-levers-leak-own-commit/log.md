
## 2026-09-01 23:49 — openzeppelin: 0.500 leaky vs 0.000 clean. The candidate's birth commit is in the history the lever reads, so the lever 'predicts' the file from the commit that created it. Fix the instrument: whereEval must hide the candidate's own commit from every history-derived signal (cochange, message affinity, birth-place) before scoring. Without this the harness cannot judge any history lever.

## 2026-09-02 01:37 — SCOPE CORRECTION (research/where-lever author, confirmed by 069 worker's audit): there is NO leak in shipped code. whereCmd's score is purely lexical; model.cochange / msgAffinity / auto.filebirth feed display text only, so selftest --where cannot exhibit the 2× inflation today. The 0.500→0.000 openzeppelin figure came from the author's uncommitted prototype message-affinity lever. 069 is therefore a HARNESS RULE, not an engine fix: (1) document in whereEval that any future history-reading signal must hide the candidate's own commit before scoring; (2) a comment beside howEval's existing fps.filter(fp => fp.sha !== C.sha) explaining why where cannot do the same cheaply (a true leave-one-out costs a full learn() per candidate — which is exactly why the harness is retrieval-not-prediction); (3) a test that asserts whereEval's scoring path reads no history-derived field, so a future lever trips it. No behaviour change, no version bump.
