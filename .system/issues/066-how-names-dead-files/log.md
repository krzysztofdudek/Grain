
## 2026-09-01 23:45 — Spec = question-catalog.md §6.5. Filter how's 'places such a change touched' to files live at HEAD — liveness set (model.pathsAll ∪ filesAll) is already computed in cochangeData and not applied here; drop the 1/N tail. Add changes/concepts to map --json (= ticket 051, fold in). Test: a deleted file never appears in how's places; map --json carries what map renders.

## 2026-09-02 03:22 — Fixed: how's places[] now filters to files live at HEAD (dead entries omitted, not marked (deleted) as ticket 020 had it — superseded) and drops the 1/N long tail by reusing how-hook's existing places.filter(p => p.k >= 2) gate, applied only when it leaves something (never drops to zero places). map --json gained concepts/changes/edges fields (additive, mirrors mapSections' text data; changes is the full uncapped model.changeArchetypes list, edges is model.moduleGraph.edges verbatim) — closes 051 too, folded in as a duplicate/subset of this ticket. Updated 2 existing tests that had locked in the old (deleted)-marking behavior (how-command.test.mjs test (f), cross-check-liveness.test.mjs's ticket-020 regression guard) to assert omission instead; added 2 new tests to cross-check-json-text.test.mjs for the map --json parity. Full suite: 2013/2013 pass, 0 fail.
