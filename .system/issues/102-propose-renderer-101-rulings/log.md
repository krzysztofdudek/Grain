
## 2026-09-05 11:00 — Zakres: (1) aspekty prozowe (content.md) renderowane jako DRAFT: brak enforce w yg-node/aspect meta albo wyraźny marker 'draft: true' zgodny ze schemą Yggdrasil (sprawdzić 'yg schemas' — nigdy nie zgadywać pola), proposal.json evidence row kind=aspect niesie status: 'draft' + reason; (2) reguły check.mjs o zasięgu plikowym wyprowadzone z konwencji pod-plikowej: provenance.json pole scopeApproximation: 'file-from-symbol' i po drillu na repo źródłowym (yg drill, jeśli YG_BIN) status DRAFT gdy FA>0; (3) no-catch (drill nie łapie żadnego mutanta) → DRAFT; (4) MIN_TYPE_FILES i flaga --min-type-files usunięte (101: nie nośne); (5) docs/reference.md sekcja 'The proposal contract' opisuje statusy active/draft i powody; (6) integration-stress.mjs i propose.test.mjs asercje na statusy; 17/17 ładowalnych musi zostać. Akceptacja: yg check na propozycji Yggdrasil ładuje się, liczba aspektów z enforce strict = tylko det. z drill 0 FA i ≥1 złapanym mutantem; suita zielona; zero zmian w engine/.

## 2026-09-05 11:11 — Started: read issue/rulings/101 report/reference.md; branch fix/102-propose-rulings rebuilt off claude/grain-agent-tool-b89y0x (7dbf8d1) after initial mistaken base on main. Plan: aspect status field (draft/advisory/enforced, yg schemas read aspect) drives shipping; evidence/provenance gain status+draftReason; provenance gains scopeApproximation for symbol-level check kinds; real yg drill run when YG_BIN resolvable to demote FA>0 -> file-scope-approximation-fa, zero-catch -> no-catch, else promote to enforced; MIN_TYPE_FILES removed; NUL-byte guard test added; docs updated.

## 2026-09-05 11:57 — propose.mjs: aspect status field (yg-aspect.yaml status: draft|advisory|enforced, yg schemas read aspect) now drives shipping. Prose always draft/prose-unenforceable-keyless. Deterministic checks verified via a REAL yg drill (when YG_BIN set) against a throwaway copy of the just-written proposal — 0 FALSE-ALARM & >=1 catch promotes to enforced; FA>0 demotes to draft/file-scope-approximation-fa; 0 catches demotes to draft/no-catch. scopeApproximation:'file-from-symbol' set in provenance.json for check-rendered aspects whose grain kind (method/type/catch/finally/case) is sub-file. evidence[]/provenance.json gain status+draftReason. Bug found+fixed on sight: node_types aspect attach sites hardcoded {id,status:'draft'}, which downgraded every newly-enforced aspect back to draft (aspect-status-downgrade, a load-blocking validator error) — switched to bare id strings so the cascade reads the aspect's own default. Second bug found+fixed on sight: tests/relations/unit/_unit-harness.mjs carried a literal NUL byte (ticket 103's pattern) in its toThrow sentinel — escaped to \x00; added tests/no-nul-bytes.test.mjs guarding plugins/grain's whole *.mjs tree. MIN_TYPE_FILES and --min-type-files removed entirely from propose.mjs and integration-stress.mjs (ticket 101 §5: not load-bearing); verified byte-identical types/nodes/aspects/alternatives counts rendering Yggdrasil's own proposal before/after. docs/reference.md 'The proposal contract' documents status/draftReason/scopeApproximation as additive fields and states the enforced-vs-candidate contract plainly. propose.test.mjs: new tests exercise promoteEnforceableAspects against a real yg drill (three real outcomes) and provenanceFor's three new fields; existing draft-only assertion loosened to draft|enforced. Full suite: 2263/2263 pass (was 2260 + 3 new).
