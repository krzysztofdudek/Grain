
## 2026-09-05 04:35 — SPEC wstępna (director). Czeka na liczby z 093 — renderer buduje się tam, gdzie rekonstrukcja już trafia: partycje+markery grup → node_types z when.path/content; cięcie modułów → yg-node.yaml z mapping; edges → relations per węzeł; archNorms exp:false → relations: default: deny lub puste listy; certyfikowane konwencje → aspekty status: draft z content.md z szablonem superpozycji jako 'what passing looks like'; deviatingSites/twins/cycles/obs pod bramką → backlog refaktoru (markdown). Wszystko jako PROPOZYCJA do katalogu poza .yggdrasil/ (np. .grain/proposal/), nigdy zapis do .yggdrasil/. Schemat wyjścia = yg schemas read node|aspect|architecture (przeczytać przed projektem).

## 2026-09-05 06:18 — RENDERER + MEASUREMENT. propose.mjs (tests/stress/): grain export -> a complete proposed .yggdrasil/ outside the repo, plus PROPOSAL.md / alternatives.md / REFACTOR-BACKLOG.md / proposal.json. Types from three levels (partition, module, directory card one level below a partition, then the uncovered layout), NESTED not an antichain — the first version carved parents hollow with not: and LOST recall (15/36 vs the 19/36 baseline). Finer candidates go to alternatives.md in both forms a hand architecture uses: a generalising content: predicate drafted from the group's marker/name-affix, and the membership frozen as an any_of list. Relations: uses: only, from resolved imports; default: deny ONLY where no observed edge contradicts it (0 emitted on Yggdrasil, both archNorms exp:false kept as class-(c) backlog per 093 §4). Cycles declared, not broken — breaking 8 edges turned one structural-cycle into four relation-undeclared-dependency. Nodes: coarse cut (no imitation of the hand graph's 250 one-file nodes). Aspects per the director's counsel steer: deterministic check.mjs wherever the enumerator class renders (43 of 215), prose only for what has no shape and each says which class; drills cut from conformingSites/deviatingSites with an optional --holdout date, always labelled. RESULTS on Yggdrasil (3019 files, 17 s from an existing export): types recall 21/36 (24 with alternatives) vs the 19/36 baseline and the 26/36 ceiling; precision 23/82; nodes recall 30/393 (24/66 on 5+-file nodes), precision 31/70; 6 of 37 hand mechanical rules named. yg check on a staged copy: graph loads, 100% coverage, ONE error — a real dependency cycle. Drill sweep of all 43 checks: 208 pass / 29 MISS / 0 FALSE-ALARM. All 7 examples PASS. Five load-failure classes found and fixed only by running the real CLI. Docs: .system/research/proposal-renderer-design.md + proposal-yggdrasil.md. Guard: tests/propose.test.mjs (11 tests, incl. the staged yg check). npm test 2205/2205.
