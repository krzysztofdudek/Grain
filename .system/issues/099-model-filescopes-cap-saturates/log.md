
## 2026-09-05 06:10 — Znalezione w 096 (too-much-yggdrasil-grain.md §6.6): model.json ogranicza fileScopes do 200 per plik; core.mjs i web-tree-sitter.js oba czytają dokładnie 200, prawdziwe 326 dla core.mjs jest w tree.json (HEAD-only, bez capu). Instrument obeszedł to czytając tree.json. Do decyzji: czy cap ma zostać (pamięć) z liczbą 'obciętych' w disclosures, czy export ma nieść prawdziwą liczbę zakresów obok obciętej listy. Klasa D (nieujawniona granica). Dogfood nr 2 z 096 — Grain certyfikuje 0 konwencji na sobie (mediana wieku zakresu 11.9 d < freshDays=14) — bramka wieku działa zgodnie z projektem, odnotowane, nie ticket.

## 2026-09-05 11:29 — Implemented: model.partitions[].fileScopesTotal (sparse, per truncated file) added in core.mjs; MODEL_V m24->m25. too-much.mjs: per-file true count now reads fileScopesTotal first, falls back to tree.json, then to the capped list (disclosed when stale); fixed an unrelated bug on sight where the size-evidence loop dropped the fileScopes fallback entirely when tree.json was absent, and a second bug where tree.json's raw list included the file's own pseudo-scope (kind 'file'), causing tree-path counts to be off-by-one vs the model-field path — both fixed so all paths agree on one true count. New fixture test tests/filescopes-total.test.mjs (9 cases, real git repo, 260-scope file generated deterministically) covers: 200 cap, fileScopesTotal correctness and sparsity, too-much.mjs with/without tree.json, stale-cache fallback disclosure, and full CLI end-to-end. npm test: 2269/2269 pass (2260 pre-existing + 9 new), 0 fail.
