
## 2026-09-02 04:01 — Found while fixing 061: the same physical catch/finally clause is walked once per enclosing body-bearing ancestor (both its method AND its enclosing class), so it is double-counted as two separate scope entries for one real clause. This inflates catch-block population counts used elsewhere (e.g. convention population sizes). Not fixed in 061 (presentation-only fix, zero extraction change) — this one changes extraction/counting and needs its own measured ticket. Fixing requires understanding the ancestor-walk dedup logic in core.mjs's extractScopes.

## 2026-09-02 04:03 — Found by the regression net added in 050, deliberately not fixed there. Two shapes: (1) Ruby singleton_method matches TYPE_LIKE_RE and is NOT a type — a false positive (class A); (2) the same 'childless companion' gap 050 fixed for Scala exists for Java/Groovy module_declaration, Ruby module, TS internal_module/module, Solidity library_declaration — bodiless/vals-only instances fall to kind method (class B). Fix each by the 050 method: census of node-types.json first, structural derivation attempted and documented, bare-token widening only with the census proving it hits type-like nodes only. Extend type-like-coverage.test.mjs so each closes red→green. EXTR_V (classification output changes).
