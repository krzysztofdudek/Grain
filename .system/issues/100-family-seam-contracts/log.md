
## 2026-09-05 06:56 — SPEC (director, z rulingu layers-compatible-no-user-thresholds). Warstwy kompatybilne = kontrakty szwów, nie wspólne repo. Po stronie Graina: (1) .grain/proposal/ jako opublikowany interfejs jak grain-export/1: schema id 'grain-proposal/1', schemaNotes, provenance.json per aspekt, sizing.json per węzeł, CORPUS.md per drills; zmiany tylko świadome i wersjonowane; (2) adapter: propozycja → format .family-candidates.json, który yg advise już czyta (v, ts, families[{id,lang,members,scope,tightness}]) — tak, żeby Yggdrasil bez żadnej zmiany kodu mógł nominować rodziny z Graina zamiast z własnego miniera (kontrakt fixture'ów planted-family = bramka precyzji, do odtworzenia po stronie Graina); (3) test szwu w suicie Graina: gdy YG_BIN dostępny — staged yg check ładuje propozycję (już w propose.test.mjs), yg drill na wyrenderowanych checkach 0 FALSE-ALARM, yg advise na kopii z .family-candidates.json z Graina pokazuje nominację; skip z powodem, gdy binary nieobecny; (4) CI Graina: job checkout sąsiada (Yggdrasil) i uruchomienie testów szwu — 'compat matrix' rodziny zaczyna się tutaj. Zależy od 097/098 (provenance, sizing).

## 2026-09-05 07:04 — DOPISEK z rulingu two-granularities: propozycja emituje charter.md per węzeł (z karty where: lives in, depends on, used by, konwencje z udziałami, egzemplarze, współzmiany) obok yg-node.yaml — Horde czyta charter.md z tej ścieżki; sizing.json z 098 jako liczba dla reguły cięcia. Alternatywy aktywne dotyczą typów i aspektów, nie węzłów.

## 2026-09-05 08:26 — propose.mjs: proposal.json now carries schema:grain-proposal/1, engine/extractor, schemaNotes (docs/reference.md, 'The proposal contract'); provenance.json per rendered aspect (same fields as 097's law-loop.mjs, without the regex — written from structured fields at render time); charter.md per proposed node beside yg-node.yaml (what lives here, depends on/used by with counts, certified conventions with share/n/exemplars, sub-gate candidates, co-change partners, sizing.json's own row, asOf sha) — 73 charters on Yggdrasil, avg 55 lines. --family-candidates adapter emits Yggdrasil's exact .family-candidates.json shape from grain's role groups with no certified convention of their own (FAMILY_MIN_MEMBERS=5, stated floor = Yggdrasil's own MIN_CLUSTER_SIZE); bug found+fixed on sight: ts must be exp.indexedAt (a real instant), not exp.asOf (a git sha) — the earlier draft had every family silently dropped by yg advise's freshness gate. Measured against a real Yggdrasil checkout staged read-only into a temp git repo (60 nodes loaded, 0 LOAD_FAILURES besides the self-referential .yggdrasil exclusion; 13 rendered checks drilled, 37 pass/16 MISS/0 FALSE-ALARM; 5 families written and all 5 nominated by yg advise) and against Yggdrasil's own family-planted-mono fixture (exactly the 1 planted family, 5 members, nothing else — the polyglot fixture folds one cross-language decoy into the TS cluster, a real precision gap named honestly, not silently patched). charter.md verified readable by Horde's real node.mjs show. New tests/seams.test.mjs (5 tests, all skip with reason when YG_BIN/HORDE_DIR absent) plus a seams CI job that checks out krzysztofdudek/Yggdrasil and krzysztofdudek/Horde, builds Yggdrasil's CLI, and runs it.

## 2026-09-05 08:34 — HUB VERIFICATION (director): diff 6 files, engine untouched, revert-test red on base (seams.test.mjs 5/5), suite 2244/2244, Yggdrasil and Horde checkouts clean. Accepted: grain-proposal/1 + schemaNotes documented in docs/reference.md; provenance.json per aspect; 73 charter.md (avg 55 lines) read back by Horde's real node.mjs; adapter emits Yggdrasil's .family-candidates.json — 5 families, yg advise nominates 5/5; planted-mono fixture exact; planted-polyglot folds one cross-language decoy (real precision gap, disclosed, not fixed — noted for 101). Fix-on-sight accepted: ts must be a Date.parse-able stamp (indexedAt), not a sha — the freshness gate silently dropped every family before. CI seams job checks out both neighbours.
