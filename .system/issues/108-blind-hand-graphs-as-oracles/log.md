
## 2026-09-05 20:59 — express oracle merged f6b0456: 14 types, 20 nodes, 15 relations, 23 aspects (20 det: 14 enforced/6 advisory; 3 LLM advisory), drill 71/71, yg check loads; 1 deliberate red (examples → lib/express). Runtime-only cycles not expressible in schema.
## 2026-09-05 19:56 — oracle petclinic: started, blind

## 2026-09-05 20:34 — oracle petclinic: DONE, blind. branch research/108-oracle-petclinic @ bf9bd17, under plugins/grain/tests/stress/oracles/spring-petclinic/. TABLE: 30 node types (2 organizational + 28 classifying; 6 enforce:strict; 4 log_required) | 27 components (7 organizational, 20 file-owning; 77 files node-owned, 54 type-level-covered, 4 excluded; coverage 135/135) | 35 relations (29 uses, 4 calls, 2 extends, 0 implements/emits/listens; default:deny on every type) | 2 flows | 28 aspects = 23 deterministic + 5 LLM, 21 enforced / 7 advisory / 0 draft | 34 drill cases over 17 aspects: 34 pass, 0 MISS, 0 FALSE-ALARM (the other 6 deterministic aspects read fs/graph or judge a whole component and report unsupported by design, verified with a probe) | CYCLES: ZERO at file, package and merged-source-root granularity over 91 import edges; the one trap is Vet<->Specialty, a cycle only if a javadoc link is counted as a reference. yg check on the staged pinned clone: loads clean, no architecture-*, no relation-undeclared-dependency, no structural-cycle, no unmapped-files, no type-strict-orphan, no log-entry-missing, no orphaned-aspect, no aspect-status-downgrade; 12 errors + 33 warnings, all unverified LLM pairs plus the 7 intended advisory refusals. check --approve --only-deterministic records 228 deterministic verdicts, 221 pass. Refactor backlog recorded with locations: 3 files with no Apache header plus 1 stale year, I18nPropertiesSyncTest singular naming, 5 untranslated fragment labels in the pet and visit forms, 5 console prints in PetClinicConcurrencyTests, PetController.updatePetDetails mutating an entity inside the controller, PetValidator restating a length limit the entity could declare.

## 2026-09-05 21:20 — petclinic oracle merged 4612e82: 30 types, 27 components, 35 relations, 28 aspects (21 enforced/7 advisory), 0 cycles, drill 34/34; trap: Vet↔Specialty cycle iff javadoc {@link} counts. Director note: the merge commit carried conflict markers in this log for one commit (union-merged right after); cause — a  in the director's command chain let the state-recording steps run after a failed merge. Chains are now one action per call.
## 2026-09-05 19:54 — oracle grain: started, blind rules acknowledged

## 2026-09-05 20:50 — oracle grain: hand-written .yggdrasil landed at plugins/grain/tests/stress/oracles/grain/. Table: 35 node types (2 organizational + 33 classifying, 26 enforce: strict), 42 nodes (7 areas + 35 owning files), 49 relations (31 calls, 18 uses, default: deny everywhere), 1 port, 0 flows, 30 aspects (22 deterministic + 8 prose; 22 enforced, 7 advisory, 1 draft), 30 drill cases across 11 aspects. Coverage 1470/1470 tracked files, every file matching exactly one type. yg check LOADS with zero load-blocking codes (no architecture-invalid, architecture-cycle, type-strict-orphan, unmapped-files, relation-undeclared-dependency, structural-cycle or port-missing-consumes). yg check --approve --only-deterministic: 1731 pairs, 1723 verified, 8 refused, 0 infra failures; every ENFORCED deterministic rule passes and all 8 refusals sit on advisory rules that record the intended backlog (3 modules over the reviewer budget, 3 files with a raw control byte, 1 instrument with no guardian test, 5 network call sites in the vendored parser runtime). yg drill: 30 pass, 0 miss, 0 false alarm, 0 unsupported. yg structure: zero dependency cycles at every depth. Blind rules held: propose/export/report never run, proposal and reconstruction artifacts and reports never read, instrument bodies never read (declared in the oracle README).

## 2026-09-05 21:21 — grain oracle merged ec8cc56: 35 types, 42 nodes, 49 relations, 30 aspects (22 enforced/7 advisory/1 draft), 0 cycles, 1470/1470 files, drill 30/30, approve 1723/1731 (8 deliberate refusals = Grain's own backlog: core.mjs 569k chars 11.4x budget, raw SOH byte core.mjs:2629 + 2 history tests, run-corpus.mjs without guardian test, 5 vendor network sites). All three oracles in tree: measurement dispatched.

## 2026-09-05 21:23 — Measurement half started (worktree research/108-measure): merged the three blind oracles, scoring grain against 4 hand graphs (grain, spring-petclinic, express, yggdrasil) via reconstruct.mjs + propose --score, building the symmetric disagreement ledger.

## 2026-09-05 21:48 — MEASURED spring-petclinic (131 files after coverage.excluded; oracle 28 classifying types / 20 mapped nodes / 35 relations / 28 aspects). reconstruct.mjs --graph oracles/spring-petclinic --export (full-history export, 106 s): types 4/28 at J>=0.5 (2 at 0.8; classes a3/b2/c19), nodes 3/20 (a0/b0/c17), relations at node granularity 4/35 — recall 0.114, precision 1.000; cycles 0 mined vs 0 declared vs 0 nominated by yg advise (agreement); aspects 1/23 named. The Vet<->Specialty trap is NOT sprung: grain emits no edge between them — but not as a win, since it emits no same-package edge anywhere on Java. Disagreement ledger, all 31 missed pairs read one by one: 25 (a) miner miss = 17 same-package simple-name references Java needs no import for + 8 test->main imports the resolver's ancestor walk never reaches; 3 (c) a controller naming its Thymeleaf template by a returned string literal; 3 (b) oracle debt = the two resources/db-scripts -> app/*/entities edges the oracle's own README calls "an edge that exists to widen a read boundary, not because the SQL depends on the Java", plus tests/web-slice -> tests/unit with no backing at all. 0 mined pairs the oracle lacks. propose: 12 types / 18 alternatives / 14 nodes / 0 relations / 0 cycles / 18 aspects (0 enforced, 9 advisory, 9 draft), 82 drill cases, lattice 270 rows with 62 in the sub-gate band; scored 2/28 types (6/28 with alternatives), precision 3/12, nodes 0/20, 0/15 hand mechanical rules named. yg check on the staged proposal: PASS, 14 nodes, 131/132 files; --approve --only-deterministic fills 224/225 (180 ok, 44 refused) — the constant advisory noise floor on untouched code that ticket 112 recorded, reproduced exactly.

## 2026-09-05 21:48 — MEASURED express (213 files, sha 023767fe; oracle 13 classifying types / 15 mapped nodes / 15 relations / 23 aspects). reconstruct: types 7/13 at J>=0.5 (5 at 0.8; a2/b0/c4), nodes 9/15 (6 at 0.8; a0/b0/c6, mean J 0.601 and 6/6 on nodes of 5+ files), relations at node granularity 13/15 — recall 0.867, precision 0.929; cycles 0 mined vs 0 nominated, and the oracle declares its static require graph acyclic, so this is agreement (the three runtime application<->prototype cycles it describes in prose are outside both sides by construction); aspects 4/20 named. Full ledger, every row read: the 2 missed pairs are package/http/{request,response} -> package/application, both (c) — neither lib/request.js nor lib/response.js requires ./application at all; the dependency is `this.app.get(...)` at request.js:231,301,341,358,388,419 and response.js:132,236,264,402 plus `this.req.app` at 895, exactly as the oracle README states. The 1 mined pair the oracle lacks is examples -> package/factory, n=2, from examples/route-map/index.js:8 and examples/route-middleware/index.js:7 both `require('../../lib/express')` — this is the oracle's DELIBERATE red, the pair its architecture denies so the built-in relation check refuses it. Grain reproduced the planted violation from evidence alone. propose: 22 types / 175 alternatives / 22 nodes / 31 relations / 0 cycles / 40 aspects (1 enforced, 4 advisory, 35 draft), 151 drill cases, lattice 1950 rows with 77 in the sub-gate band; scored 6/13 types (no alternative adds one), precision 7/22, nodes 6/15 recall and 7/22 precision, 1/5 hand mechanical rules named. yg check on the staged proposal: loads clean — 22 nodes, 213/213 files, 40 aspects, no structural-cycle, no unmapped-files; --approve --only-deterministic fills 356/358 (245 ok, 111 refused), leaving 6 errors and 107 warnings. This is the one repository of the four with no dependency gate in its CI, and its relation precision is still 0.929.

## 2026-09-05 21:48 — MEASURED grain-on-itself (1470 files at 3d249bf, the commit the oracle was written against; oracle 33 classifying types / 35 mapped nodes / 49 relations / 30 aspects). Run on a clone under scratch, never in the worktree. reconstruct: types 18/33 at J>=0.5 (14 at 0.8; a8/b1/c6), nodes 28/35 (24 at 0.8, mean J 0.783 — the best node recovery of the four; a0/b1/c6), relations at node granularity 34/49 — recall 0.694, precision 1.000; cycles 0 mined vs 0 nominated vs 0 declared (the oracle README says a recovered cycle here would be a false positive — none was); aspects 2/22 named. Ledger, all 15 missed pairs read: 1 (a) plugin/engine/propose -> plugin/engine/core, a dynamic import assembled from a literal (propose.mjs:54 `resolve(here, 'core.mjs')`, :704 `await import(...)`); 8 (b) — seven the oracle's own node files mark "No code dependency ... declared so <rule> may read <target>" (commands->dispatch, hooks->dispatch, grammars->config, manifests->config, marketplace->manifests, readme->docs, readme->config) and one genuine oracle error, repo/fixture -> plugin/entry, where tests/fixtures/build-fixture.mjs never invokes grain at all; 6 (c) reached by a spawn or through an intermediary (tests/stress->entry, repo/instruments->entry, tests/suite->engine/export, tests/relations/unit->{engine/core, vendor-relations} through _unit-harness.mjs which belongs to the harness node, director/tools->director/state by path). 0 mined pairs the oracle lacks. THE SHARPEST FINDING IS NOT A SCORE: `grain export` on grain's own repository certifies ZERO conventions ("indexed in 230.9s: 0 conventions, 7346 scopes"), against 149 on Yggdrasil, 26 on express and 6 on petclinic. Every one of the 22 checks propose renders therefore comes from the sub-gate lattice, so `enforced` is 0 by the certified-origin ruling, and the aspect comparison has almost nothing to compare. propose: 31 types / 190 alternatives / 34 nodes / 22 relations / 0 cycles / 81 aspects (0 enforced, 22 advisory, 59 draft), 338 drill cases, lattice 5453 rows with 430 in the sub-gate band; scored 15/33 types (21/33 with alternatives — the best alternative lift of the four), precision 18/31, nodes 15/35 and 18/31, 0/15 hand mechanical rules named. yg check on the staged proposal: PASS, 34 nodes, 1466/1470 files (100%); --approve --only-deterministic fills 616/616 with 216 ok and 400 REFUSED — a 65% refusal rate on untouched code, against 31% on express and 20% on petclinic, and 396 of the 400 land on plugins/grain/tests/relations/unit/**, the 880-file relation fixture corpus. On its own repository the miner's dominant practice is its test fixtures.

## 2026-09-05 22:07 — MEASUREMENT DONE — branch research/108-measure, 4 commits (be20a89 reconstruct --graph; 9351ba9 propose --score content root; 36b5c13 the oracle guard tests + CI; ac44a5f the memo and the results file). Deliverables: .system/research/oracles-4-measurement.md and plugins/grain/tests/stress/results/oracles-4-2026-09-05.json (schema in its own `schema` field; .gitignore now keeps oracles-*.json, which is cited by number in a memo rather than per-run machine noise).

AGGREGATE, reconstruction (files | oracle types/nodes/relations/aspects | type J>=0.5 | node J>=0.5 | relation recall | precision | cycles | aspects named):
  grain 1470 | 33/35/49/30 | 18/33 | 28/35 | 0.694 | 1.000 | 0 vs 0 | 2/22
  spring-petclinic 131 | 28/20/35/28 | 4/28 | 3/20 | 0.114 | 1.000 | 0 vs 0 | 1/23
  express 213 | 13/15/15/23 | 7/13 | 9/15 | 0.867 | 0.929 | 0 vs 0 | 4/20
  Yggdrasil 3019 | 36/393/1261/70 | 19/36 | 83/393 | 0.894 | 0.998 | 2 mined vs 1 advise | 11/57
The Yggdrasil row reproduces ticket 093 to the number, so the four rows are commensurable.

DISAGREEMENT CLASS TOTALS (a miner miss / b graph debt / c undecidable), every row read in the source:
  types below J 0.5   grain 8/1/6 · petclinic 3/2/19 · express 2/0/4 · Yggdrasil 7/2/8  = 20/5/37
  nodes below J 0.5   0/2/338 across the four (class a is zero by construction)
  relations the oracle has and grain lacks   grain 1/8/6 · petclinic 25/3/3 · express 0/0/2 · Yggdrasil 0/0/131  = 26/11/142
  relations grain has and the oracle lacks   3 in total across 1335 declared pairs — and the one on express is the violation the oracle deliberately left undeclared so its own check would refuse it.

REPRESENTATIVENESS: precision was the sound number; recall was not. Precision is 0.929–1.000 on all four and express carries no dependency gate in CI at all, so 0.998 was NOT the import-gate ceiling the memos feared. Recall is a function of how much of the dependency graph is written as a resolvable import: 0.894 TypeScript, 0.867 CommonJS, 0.694 JS with spawns and read-boundary edges, 0.114 Java.

THREE FINDINGS BEYOND THE SCORES. (1) Ticket 113 measured and logged there with file:line — the Java loss is three-stage and the module stage is structural, not a threshold. (2) grain export on grain's own repository certifies ZERO conventions (149 on Yggdrasil, 26 express, 6 petclinic), so every rule it proposes there is sub-gate and `enforced` can never be non-empty — belongs to 117. (3) The adoption noise floor is one population: 20%–65% of deterministic verdicts refuse on untouched code, and 35 of 44 (petclinic), 102 of 112 (express) and 398 of 400 (grain) of those refusals are rules in the `false` direction — "files here do NOT import X", "cases here do NOT call `it`". That is ticket 115 and it is worth more than any recall point on the table.

RECOMMENDATIONS WITH NUMBERS are in §9 of the memo, one paragraph each for 110, 113, 114, 115, 116.

INSTRUMENT: two bugs fixed, each with a test that was red first — reconstruct.mjs assumed the graph lives inside the repo it describes (no oracle could be used at all), and propose --score evaluated a hand `content:` predicate against the graph directory, silently dropping every content-gated type out of the recall denominator (18 such predicates in petclinic alone). No mining change. tests/reconstruct.test.mjs now runs against the express and spring-petclinic oracles when GRAIN_CORPUS_CLONES is set and skips with a reason when it is not; the CI seams job checks both repositories out at the exact oracle commits and runs it.
