
## 2026-09-05 04:35 — SPEC (director). Cel: liczba, nie anegdota — jaki ułamek ręcznie napisanego .yggdrasil/ Grain odzyskuje sam. Wejście: repo z .yggdrasil/ (wzorzec) + grain export tego repo. Porównania: (a) TYPY: każdy klasyfikujący node_type (when.path/content) → zbiór plików; każda partycja Graina i każdy moduł (moduleGraph.nodes) → zbiór plików; najlepszy Jaccard per typ, recall@Jaccard≥0.5; (b) WĘZŁY: każde yg-node.yaml mapping → zbiór plików vs moduły/grupy Graina; (c) RELACJE: deklarowane relacje węzłów zagregowane do par (moduł→moduł) vs Grain edges zagregowane tak samo — precision/recall par; osobno archNorms 'false' vs relations default: deny w architekturze; (d) CYKLE: Grain cycles vs nominacje 'architecture cut' z yg advise; (e) ASPEKTY: dla każdego aspektu deterministycznego z check.mjs, czy jakakolwiek konwencja/grupa Graina na tym samym zbiorze plików nazywa ten sam identyfikator (import/call) — tylko tam, gdzie da się to zmierzyć mechanicznie, reszta jako 'niemierzalne' z liczbą. Wyjście: instrument plugins/grain/tests/stress/reconstruct.mjs (zero zmian silnika), JSON + tabela markdown; raport .system/research/reconstruction-yggdrasil.md. Wzorce: /home/user/Yggdrasil (1564 commity, 427 węzłów, 33 typy, 51 aspektów) + examples/* (7 mini-grafów, smoke). Yggdrasil read-only: po pomiarze usunąć .grain/ z tego repo.

## 2026-09-05 04:43 — KOREKTA SPEC (user, 2026-09-05): wzorzec jest omylny. Nie scorować każdego rozjazdu jako chybienia Graina — raportować symetrycznie: (a) Grain nie widzi, (b) graf ręczny odbiega od praktyki kodu, (c) nierozstrzygalne. Dołączyć listę (b) jako 'dług grafu Yggdrasila' (informacyjnie). Przekazane workerowi przez SendMessage.
## 2026-09-05 05:08 — MEASURED (Opus worker). Instrument plugins/grain/tests/stress/reconstruct.mjs + guard plugins/grain/tests/reconstruct.test.mjs (13 tests) + report .system/research/reconstruction-yggdrasil.md. Zero engine changes. Dependency-free YAML subset parser validated against the real yaml package over 830 graph/workflow/fixture files: 830 identical, 0 differ, 0 throw (three parser bugs found and fixed by that diff and by the guard test). Module assignment imports refineModOf so module ids are byte-identical to grain's own; self-check moduleAssignmentMismatch 0/37. YGGDRASIL (3019 files, 38 types, 427 nodes, 70 aspects; grain export cold 434 s, instrument 2.5 s): (a) types 19/36 at J>=0.5, 12 at >=0.8, mean 0.55 - 7 class-a, 2 class-b, 8 class-c; (b) node mappings 83/393, but 250 nodes map ONE file (stratified: 5+ files -> 28/66, mean 0.458); (c) module pairs 61/68 recall 0.897 precision 0.938, and at the graph's own node granularity 1105/1236 recall 0.894 precision 0.998; archNorms exp:false 2 - 1 agrees with default: deny, 1 contradicts an allow-list; (d) cycles 2/2, recall 1.0; (e) deterministic aspects 11/57 named - 20 miner miss, 6 invisible by construction (rules about an absence), 20 unmeasurable (no literal at all), 13 prose out of scope. EXAMPLES (7, 0.39-0.42 s each): types 13/14, nodes 8/16, node-level relations 8/8, aspects 0/6 - and zero partitions and zero conventions on every one, so they are smoke, not evidence. NEXT TICKETS named in the report: seven node types grain already HAS as a directory card or role group but never surfaces as a type candidate (portal-server J=1.00, portal-frontend-vendor 1.00, portal-engine-api 0.90, portal-frontend-core, llm-provider, parser-adapter, reviewer-dispatch) - promoting those levels moves type recall 19/36 -> up to 26/36 with no new mining; and the 20 aspect identifiers that sit in plain sight below the lambda gate. Two headline numbers must not be quoted alone: relation precision 0.998 measures Yggdrasil's own enforced undeclared-dependency check, and 9 of the 12 strongest type matches are one-directory types where both sides just read the layout. Yggdrasil left read-only: .grain removed, no tracked file touched.
