## [2026-09-05T20:25:50.844Z]
Only the head of the owner unit gets a repository, and the pet-type lookup gets one because a pet type is shared reference data that owners merely point at rather than part of any owner. Giving pets or visits their own repository would create a second, shorter path to the database that skips the owner and therefore skips the consistency the unit guarantees; the shorter path is always the one someone reaches for under time pressure, so it is not offered at all. Owner search is by surname prefix and returns a page rather than a list because the clinic's owner table grows without bound and the search screen is the busiest page in the application.
