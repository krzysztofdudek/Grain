name: Vendored Relation Machinery
type: vendored-relations
description: >-
  The extractors, symbol table, resolver and per-language path resolvers, mechanically vendored from a
  sibling project under the same licence and the same author, committed as plain modules with no runtime
  dependency. Regenerated by a script, never edited by hand: a hand fix here survives exactly until the
  next regeneration and then vanishes with nothing in the diff to explain it.
mapping:
  - plugins/grain/engine/vendor/relations/
