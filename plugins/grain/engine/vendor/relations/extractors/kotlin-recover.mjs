// GENERATED by scripts/build-relations.mjs — do not edit.
// Source: Yggdrasil 1f82edbb6 source/cli/src/relations/extractors/kotlin-recover.ts (MIT, same author). Regenerate: node scripts/build-relations.mjs
import { Parser } from '../../web-tree-sitter/web-tree-sitter.js';
const IDENT_START = /[A-Za-z_À-￿]/;
const IDENT_PART = /[A-Za-z0-9_À-￿]/;
function tokenize(text) {
    const toks = [];
    let i = 0;
    let line = 0;
    let lineStart = 0;
    let unbalanced = false;
    const n = text.length;
    const push = (kind, start, end, extra)=>{
        toks.push({
            kind,
            start,
            end,
            text: text.slice(start, end),
            line,
            col: start - lineStart,
            ...extra
        });
    };
    const newlineAt = (pos)=>{
        line++;
        lineStart = pos + 1;
    };
    const skipString = (from, raw, dollars)=>{
        let j = from;
        while(j < n){
            const c = text[j];
            if (c === '\n') {
                newlineAt(j);
                j++;
                continue;
            }
            if (!raw && c === '\\') {
                j += 2;
                continue;
            }
            if (c === '"') {
                if (!raw) return j + 1;
                if (text.startsWith('"""', j)) {
                    let k = j + 3;
                    while(k < n && text[k] === '"')k++;
                    return k;
                }
                j++;
                continue;
            }
            if (c === '$') {
                let k = j;
                while(k < n && text[k] === '$')k++;
                if (k - j >= dollars && text[k] === '{') {
                    j = skipTemplate(k + 1);
                    continue;
                }
                j = k;
                continue;
            }
            j++;
        }
        unbalanced = true;
        return n;
    };
    const skipTemplate = (from)=>{
        let depth = 1;
        let j = from;
        while(j < n){
            const c = text[j];
            if (c === '\n') {
                newlineAt(j);
                j++;
                continue;
            }
            if (c === '"') {
                const raw = text.startsWith('"""', j);
                j = skipString(j + (raw ? 3 : 1), raw, 1);
                continue;
            }
            if (c === '{') depth++;
            else if (c === '}') {
                depth--;
                if (depth === 0) return j + 1;
            }
            j++;
        }
        unbalanced = true;
        return n;
    };
    while(i < n){
        const c = text[i];
        if (c === '\n') {
            push('nl', i, i + 1);
            newlineAt(i);
            i++;
            continue;
        }
        if (c === ' ' || c === '\t' || c === '\r' || c === '\f') {
            i++;
            continue;
        }
        if (c === '/' && text[i + 1] === '/') {
            while(i < n && text[i] !== '\n')i++;
            continue;
        }
        if (c === '/' && text[i + 1] === '*') {
            let depth = 1;
            i += 2;
            while(i < n && depth > 0){
                if (text[i] === '\n') newlineAt(i);
                if (text[i] === '/' && text[i + 1] === '*') {
                    depth++;
                    i += 2;
                    continue;
                }
                if (text[i] === '*' && text[i + 1] === '/') {
                    depth--;
                    i += 2;
                    continue;
                }
                i++;
            }
            if (depth > 0) unbalanced = true;
            continue;
        }
        if (c === '$') {
            let k = i;
            while(k < n && text[k] === '$')k++;
            if (text[k] === '"') {
                const startLine = line;
                const startCol = i - lineStart;
                const raw = text.startsWith('"""', k);
                const end = skipString(k + (raw ? 3 : 1), raw, k - i);
                toks.push({
                    kind: 'str',
                    start: i,
                    end,
                    text: text.slice(i, end),
                    line: startLine,
                    col: startCol,
                    dollars: k - i
                });
                i = end;
                continue;
            }
            push('p', i, k);
            i = k;
            continue;
        }
        if (c === '"') {
            const startLine = line;
            const startCol = i - lineStart;
            const raw = text.startsWith('"""', i);
            const end = skipString(i + (raw ? 3 : 1), raw, 1);
            toks.push({
                kind: 'str',
                start: i,
                end,
                text: text.slice(i, end),
                line: startLine,
                col: startCol,
                dollars: 1
            });
            i = end;
            continue;
        }
        if (c === "'") {
            let j = i + 1;
            while(j < n && text[j] !== "'" && text[j] !== '\n')j += text[j] === '\\' ? 2 : 1;
            push('str', i, Math.min(j + 1, n));
            i = Math.min(j + 1, n);
            continue;
        }
        if (c === '`') {
            let j = i + 1;
            while(j < n && text[j] !== '`' && text[j] !== '\n')j++;
            push('id', i, Math.min(j + 1, n));
            i = Math.min(j + 1, n);
            continue;
        }
        if (IDENT_START.test(c) || /[0-9]/.test(c)) {
            let j = i + 1;
            while(j < n && IDENT_PART.test(text[j]))j++;
            push('id', i, j);
            i = j;
            continue;
        }
        if (c === '-' && text[i + 1] === '>') {
            push('p', i, i + 2);
            i += 2;
            continue;
        }
        push('p', i, i + 1);
        i++;
    }
    return {
        toks,
        unbalanced
    };
}
const OPEN = new Set([
    '(',
    '[',
    '{'
]);
const CLOSE = new Set([
    ')',
    ']',
    '}'
]);
const MODIFIERS = new Set([
    'public',
    'private',
    'protected',
    'internal',
    'open',
    'final',
    'abstract',
    'sealed',
    'data',
    'enum',
    'annotation',
    'inner',
    'value',
    'inline',
    'noinline',
    'crossinline',
    'override',
    'lateinit',
    'const',
    'suspend',
    'tailrec',
    'operator',
    'infix',
    'external',
    'expect',
    'actual',
    'companion',
    'vararg',
    'reified',
    'out',
    'in'
]);
const DECL_KEYWORDS = new Set([
    'class',
    'interface',
    'object',
    'fun',
    'val',
    'var',
    'typealias',
    'constructor',
    'init',
    'import',
    'package'
]);
function matching(toks, open) {
    let depth = 0;
    for(let j = open; j < toks.length; j++){
        const t = toks[j];
        if (t.kind !== 'p') continue;
        if (OPEN.has(t.text)) depth++;
        else if (CLOSE.has(t.text)) {
            depth--;
            if (depth === 0) return j;
        }
    }
    return -1;
}
function nextReal(toks, from) {
    for(let j = from; j < toks.length; j++)if (toks[j].kind !== 'nl') return j;
    return -1;
}
function startsDeclarationLine(toks, idx) {
    for(let j = idx - 1; j >= 0; j--){
        const t = toks[j];
        if (t.kind === 'nl') return true;
        if (t.kind === 'id' && MODIFIERS.has(t.text)) continue;
        return false;
    }
    return true;
}
function blankRanges(toks) {
    const out = [];
    for(let i = 0; i < toks.length; i++){
        const t = toks[i];
        if (t.kind === 'str' && (t.dollars ?? 1) >= 2) out.push([
            t.start,
            t.start + (t.dollars ?? 0)
        ]);
        if (t.kind === 'id' && t.text === 'context' && startsDeclarationLine(toks, i)) {
            const open = i + 1;
            if (toks[open]?.kind === 'p' && toks[open].text === '(') {
                const close = matching(toks, open);
                const after = close === -1 ? -1 : nextReal(toks, close + 1);
                if (after !== -1 && (toks[after].kind === 'id' || toks[after].text === '@')) {
                    out.push([
                        t.start,
                        toks[close].end
                    ]);
                }
            }
        }
    }
    const stack = [];
    let pendingWhen = false;
    let whenParenDepth = 0;
    for(let i = 0; i < toks.length; i++){
        const t = toks[i];
        const top = stack[stack.length - 1];
        if (t.kind === 'id' && t.text === 'when') {
            pendingWhen = true;
            whenParenDepth = 0;
            continue;
        }
        if (t.kind === 'p' && OPEN.has(t.text)) {
            if (pendingWhen && t.text === '(') whenParenDepth++;
            const isWhenBody = pendingWhen && t.text === '{' && whenParenDepth === 0;
            if (isWhenBody) pendingWhen = false;
            stack.push({
                brace: t.text,
                when: isWhenBody,
                inCond: isWhenBody,
                guardAt: -1,
                sawElseAfterIf: false
            });
            continue;
        }
        if (t.kind === 'p' && CLOSE.has(t.text)) {
            if (pendingWhen && t.text === ')' && whenParenDepth > 0) whenParenDepth--;
            stack.pop();
            continue;
        }
        if (pendingWhen && whenParenDepth === 0 && t.kind !== 'nl') {
            if (!(t.kind === 'p' && t.text === '(')) pendingWhen = false;
        }
        if (top === undefined || !top.when) continue;
        if (top.inCond) {
            if (t.kind === 'id' && t.text === 'if' && top.guardAt === -1) {
                top.guardAt = i;
                top.sawElseAfterIf = false;
                continue;
            }
            if (t.kind === 'id' && t.text === 'else' && top.guardAt !== -1) {
                top.sawElseAfterIf = true;
                continue;
            }
            if (t.kind === 'p' && t.text === '->') {
                if (top.guardAt !== -1 && !top.sawElseAfterIf) out.push([
                    toks[top.guardAt].start,
                    t.start
                ]);
                top.inCond = false;
                top.guardAt = -1;
                continue;
            }
            if (t.kind === 'nl') top.guardAt = -1;
        } else if (t.kind === 'nl' || t.kind === 'p' && t.text === ';') {
            top.inCond = true;
            top.guardAt = -1;
        }
    }
    return out;
}
function applyBlanks(text, ranges) {
    if (ranges.length === 0) return text;
    const chars = text.split('');
    for (const [s, e] of ranges)for(let k = s; k < e; k++)if (chars[k] !== '\n') chars[k] = ' ';
    return chars.join('');
}
function chunkSpan(toks, spanStart, spanEnd) {
    const chunks = [];
    let depth = 0;
    let cur;
    let prefixOnly = true;
    let lineHasTok = false;
    for (const t of toks){
        if (t.start < spanStart || t.start >= spanEnd) continue;
        if (t.kind === 'nl') {
            lineHasTok = false;
            continue;
        }
        const firstOnLine = !lineHasTok;
        lineHasTok = true;
        if (depth === 0 && firstOnLine && t.col === 0 && !(t.kind === 'p' && CLOSE.has(t.text)) && (cur === undefined || !prefixOnly)) {
            if (cur !== undefined) chunks.push(cur);
            cur = {
                start: t.start,
                end: t.end,
                toks: []
            };
            prefixOnly = true;
        }
        if (cur === undefined) {
            cur = {
                start: t.start,
                end: t.end,
                toks: []
            };
            prefixOnly = true;
        }
        cur.toks.push(t);
        cur.end = t.end;
        if (depth === 0 && prefixOnly) {
            const isPrefixTok = t.kind === 'p' && (t.text === '@' || t.text === '.' || t.text === ':' || OPEN.has(t.text)) || t.kind === 'id' && !DECL_KEYWORDS.has(t.text);
            if (!isPrefixTok) prefixOnly = false;
        }
        if (t.kind === 'p' && OPEN.has(t.text)) depth++;
        else if (t.kind === 'p' && CLOSE.has(t.text)) depth = Math.max(0, depth - 1);
    }
    if (cur !== undefined) chunks.push(cur);
    return {
        chunks,
        balanced: depth === 0
    };
}
function skipAngles(toks, i) {
    let depth = 0;
    for(let j = i; j < toks.length; j++){
        const t = toks[j];
        if (t.kind === 'p' && t.text === '<') depth++;
        else if (t.kind === 'p' && t.text === '>') {
            depth--;
            if (depth === 0) return j + 1;
        } else if (t.kind === 'p' && (t.text === '{' || t.text === '=')) return j;
    }
    return toks.length;
}
function readHeader(chunk) {
    const toks = chunk.toks.filter((t)=>t.kind !== 'nl');
    let i = 0;
    while(i < toks.length){
        const t = toks[i];
        if (t.kind === 'p' && t.text === '@') {
            i++;
            if (toks[i]?.kind === 'p' && toks[i].text === '[') {
                const m = matching(toks, i);
                if (m === -1) return undefined;
                i = m + 1;
                continue;
            }
            while(toks[i]?.kind === 'id'){
                i++;
                if (toks[i]?.kind === 'p' && (toks[i].text === '.' || toks[i].text === ':')) {
                    i++;
                    continue;
                }
                break;
            }
            if (toks[i]?.kind === 'p' && toks[i].text === '<') i = skipAngles(toks, i);
            if (toks[i]?.kind === 'p' && toks[i].text === '(') {
                const m = matching(toks, i);
                if (m === -1) return undefined;
                i = m + 1;
            }
            continue;
        }
        if (t.kind === 'id' && MODIFIERS.has(t.text)) {
            i++;
            continue;
        }
        break;
    }
    const kw = toks[i];
    if (kw === undefined || kw.kind !== 'id') return undefined;
    const line = kw.line + 1;
    const nameAt = (j)=>toks[j]?.kind === 'id' ? toks[j].text.replace(/`/g, '') : undefined;
    if (kw.text === 'class' || kw.text === 'interface' || kw.text === 'object') {
        const name = nameAt(i + 1);
        return name === undefined ? undefined : {
            name,
            kind: 'type',
            line
        };
    }
    if (kw.text === 'typealias') {
        const name = nameAt(i + 1);
        return name === undefined ? undefined : {
            name,
            kind: 'callable',
            line
        };
    }
    if (kw.text === 'fun' && toks[i + 1]?.kind === 'id' && toks[i + 1].text === 'interface') {
        const name = nameAt(i + 2);
        return name === undefined ? undefined : {
            name,
            kind: 'type',
            line
        };
    }
    if (kw.text === 'fun' || kw.text === 'val' || kw.text === 'var') {
        let j = i + 1;
        if (toks[j]?.kind === 'p' && toks[j].text === '<') j = skipAngles(toks, j);
        if (kw.text !== 'fun' && toks[j]?.kind === 'p' && toks[j].text === '(') return null;
        const terminators = kw.text === 'fun' ? new Set([
            '('
        ]) : new Set([
            ':',
            '=',
            '{',
            ';'
        ]);
        let angle = 0;
        let last;
        for(; j < toks.length; j++){
            const t = toks[j];
            if (t.line !== kw.line && kw.text !== 'fun') break;
            if (t.kind === 'p' && t.text === '<') {
                angle++;
                continue;
            }
            if (t.kind === 'p' && t.text === '>') {
                angle--;
                continue;
            }
            if (angle > 0) continue;
            if (t.kind === 'p' && terminators.has(t.text)) break;
            if (t.kind === 'id' && kw.text !== 'fun' && t.text === 'by') break;
            if (t.kind === 'p' && t.text === '(') return undefined;
            if (t.kind === 'id') last = t.text.replace(/`/g, '');
            else if (!(t.kind === 'p' && (t.text === '.' || t.text === '?' || t.text === ','))) return undefined;
        }
        return last === undefined ? undefined : {
            name: last,
            kind: 'callable',
            line
        };
    }
    return undefined;
}
function coveringNodes(chunk, clean) {
    const used = [];
    for (const t of chunk.toks){
        if (t.kind === 'nl') continue;
        let lo = 0;
        let hi = clean.length - 1;
        let at = -1;
        while(lo <= hi){
            const mid = lo + hi >> 1;
            if (clean[mid].startIndex <= t.start) {
                at = mid;
                lo = mid + 1;
            } else hi = mid - 1;
        }
        const c = at === -1 ? undefined : clean[at];
        if (c === undefined || t.end > c.endIndex) return undefined;
        if (!used.includes(c)) used.push(c);
    }
    return used;
}
function damaged(node) {
    if (!node.hasError) return false;
    if (node.type === 'ERROR') return true;
    return node.descendantsOfType('ERROR').length > 0;
}
function parseAt(parser, text, start, end) {
    const before = text.slice(0, start);
    const rows = (before.match(/\n/g) ?? []).length;
    const col = start - (before.lastIndexOf('\n') + 1);
    return parser.parse('\n'.repeat(rows) + ' '.repeat(col) + text.slice(start, end) + '\n');
}
export function kotlinView(tree, content) {
    const root = tree.rootNode;
    if (!damaged(root)) {
        return {
            roots: [
                root
            ],
            lexical: [],
            packageIncomplete: false,
            incompleteTypes: new Set(),
            dispose: ()=>{}
        };
    }
    const owned = [];
    const parser = new Parser();
    let disposed = false;
    const dispose = ()=>{
        if (disposed) return;
        disposed = true;
        for (const t of owned)t.delete();
        parser.delete();
    };
    try {
        parser.setLanguage(tree.language);
        const { toks, unbalanced: lexUnbalanced } = tokenize(content);
        const text = applyBlanks(content, blankRanges(toks));
        let base = tree;
        if (text !== content) {
            const reparsed = parser.parse(text);
            if (reparsed !== null) {
                owned.push(reparsed);
                base = reparsed;
            }
        }
        const view = {
            roots: [],
            lexical: [],
            packageIncomplete: false,
            incompleteTypes: new Set(),
            dispose
        };
        if (!damaged(base.rootNode)) {
            view.roots.push(base.rootNode);
            return view;
        }
        if (lexUnbalanced) view.packageIncomplete = true;
        const baseToks = base === tree ? toks : tokenize(text).toks;
        const clean = [];
        for(let i = 0; i < base.rootNode.childCount; i++){
            const c = base.rootNode.child(i);
            if (c !== null && !damaged(c)) clean.push(c);
        }
        const { chunks, balanced } = chunkSpan(baseToks, 0, text.length);
        if (!balanced) view.packageIncomplete = true;
        const taken = new Set();
        for (const chunk of chunks){
            const covering = coveringNodes(chunk, clean);
            if (covering !== undefined) {
                for (const c of covering)if (!taken.has(c.id)) {
                    taken.add(c.id);
                    view.roots.push(c);
                }
                continue;
            }
            const t = parseAt(parser, text, chunk.start, chunk.end);
            if (t !== null) owned.push(t);
            if (t !== null && !damaged(t.rootNode)) {
                view.roots.push(t.rootNode);
                continue;
            }
            const header = readHeader(chunk);
            if (header === undefined) {
                view.packageIncomplete = true;
                continue;
            }
            if (header === null) continue;
            view.lexical.push(header);
            if (header.kind === 'type') view.incompleteTypes.add(header.name);
        }
        return view;
    } catch (err) {
        dispose();
        throw err;
    }
}
