// GENERATED by scripts/build-relations.mjs — do not edit.
// Source: Yggdrasil dcc9f057 source/cli/src/relations/extractors/include-resolve.ts (MIT, same author). Regenerate: node scripts/build-relations.mjs
import path from 'node:path';
export function resolveIncludePath(headerPath, fromFile, exists) {
    if (headerPath === '') return undefined;
    const fromDir = path.posix.dirname(toPosix(fromFile));
    const relative = normalizeRepoRel(path.posix.join(fromDir, headerPath));
    if (relative !== undefined && exists(relative)) return relative;
    return undefined;
}
function normalizeRepoRel(p) {
    const norm = path.posix.normalize(p);
    if (norm.startsWith('..')) return undefined;
    return norm;
}
function toPosix(p) {
    return p.replace(/\\/g, '/');
}
